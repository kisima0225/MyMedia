# MyMedia 实施计划总览与交接

> 日期：2026-08-17，末次核对 2026-08-27（Task 15 完成后）　　用途：跨会话交接。
> **本文件是唯一入口**：新窗口先读它，再读要执行的那份计划。
> （原先并存的 `STATUS.md` 已并入本文件并删除——两份「第一入口」互相指认，
> 结果是其中一份必然过期，2026-08-19 核对时它已经落后两个计划。）

## 1. 计划切分（8 份，对应 spec §11 路线图 P0–P13）

| 计划 | 覆盖 | 文件 | 状态 |
|---|---|---|---|
| 01 基础设施 | P0–P2 骨架 / 认证 / 媒体库 / 任务队列 | `2026-08-17-01-infrastructure.md` | ✅ 已执行并合入 `main` |
| 02 扫描框架 | P3 对账 / 采样哈希 / 改名检测 / SPI | `2026-08-17-02-scanning.md` | ✅ 已执行并合入 `main`（tag `plan-02-scanning`） |
| 03 视频域 | P4–P5 语义模型 / 文件名解析 / 目录树 / Range 播放 / 进度 | `2026-08-17-03-video-domain.md` | ✅ 已执行并合入 `main`（10 任务） |
| ↳ 扫描修复 | 计划 02 review 带出的 4 项 Important：扫描长事务 / 任务租约续期 / 命名接口 / 内容变化回调 | `2026-08-18-scanning-important-fixes.md` | ✅ 已执行并合入 `main`（`a8bc946`） |
| 04 图片域 | P6–P7 节点树 / CBZ 索引与阅读 / 阅读模式 / 阅读进度 | `2026-08-17-04-image-domain.md` | ✅ 已执行并合入 `main`（10 任务，tag `plan-04-image_domain`） |
| 05 预览与元数据 | P8–P9 ffprobe / 封面 / 缩略图 / 雪碧图 / 刮削链 / 候选队列 | `2026-08-17-05-preview-metadata.md` | ✅ 已执行并合入 `main`（12 任务，tag `plan-05-preview_metadata`） |
| 06 检索与上传 | P10–P11 搜索 / 标签 / 收藏 / 分享链接 / 分片上传 | `2026-08-17-06-search-and-upload.md` | ✅ 已执行并合入 `main`（13 任务，tag `plan-06-search_and_upload`） |
| **07 前端** | **P12（已调 `frontend-design` 技能）** | `2026-08-17-07-frontend.md` | ✅ 已执行：全部 19 个任务（A–E 五段）完成，逐任务 review clean；全分支 whole-branch final review 已做（2 Critical + 1 Important + 6 Minor）且唯一一轮修复已提交并经 scoped re-review 确认全部 ADDRESSED（4 commits，无残留 Critical/Important），worktree `D:\MyMedia-7`（分支 `plan07-2026-08-17-07-frontend`），**尚未合入 `main`**，合并方式待决定，见 §19 |
| 08 交付 | P13 seed 数据 / README / ADR / 讲解文档 | 待写 | ⬜ |

**代码进度**：计划 01–06 与那份扫描修复都已执行并合入 `main`。`main` 的 `src/` 下已有
`shared` / `user` / `library` / `jobs` / `scan` / `video` / `image` / `preview` / `metadata` /
`web` / `upload` 十一个模块与 V1–V16 迁移。计划 05 新增 `preview` 与 `metadata` 模块、V10–V11
迁移、事件接线、扫描后补齐、派生资源端点、字段合并与领域写回、本地元数据提供者、文件名兜底、
提供者链与 Bangumi/TMDB 外部提供者、候选队列；合并提交 `2e8c750`，tag `plan-05-preview_metadata`。
计划 06 新增双路径搜索、标签、收藏、分享链接、分片上传/断点续传/秒传；合并提交 `cb691cb`，
tag `plan-06-search_and_upload`。两份计划的逐任务提交链与验证数字见 §10、§11。计划 07
的全部 19 个任务（A–E 五段）都已在独立 worktree `D:\MyMedia-7`（分支
`plan07-2026-08-17-07-frontend`）上执行完成，**尚未合入 `main`**——`main` 目前仍停在计划 06
合并后的状态，前端骨架、`frontend-maven-plugin` 构建集成、视频域/图片域/跨域管理界面都只
存在于这个 worktree。详见 §14/§15/§16/§17/§18。

> **⚠ 扫描修复改了跨计划契约，别按老印象写代码。** `LibraryContentBuilder` 从两个回调
> 变成三个（新增 `onFileChanged`），跨模块事件走 `scan :: events` 命名接口。
> 计划 04、05 的正文已于 2026-08-19 按此对齐，但**再往后写计划 07/08 时要记得这份修复存在**。

## 2. 跨计划已固化的约定

违反这些约定会让后续计划对不上，改之前先想清楚。

- **迁移编号**：V1–V4（01，含补丁 `V1_1__event_publication.sql`）、V5（02）、V6–V7（03）、V8–V9（04）、V10–V11（05）、V12–V16（06）、**V17 起留给 07/08**。计划 06 起执行一条更严的规矩：**一支迁移只由一个任务创建**，否则第二个任务就得改一支已经跑过的迁移，Flyway 会因校验和不符拒绝启动。
- **任务类型**：`LIBRARY_SCAN`(02)、`ARCHIVE_INDEX`(04)、`PREVIEW_GENERATE` / `SPRITE_GENERATE` / `METADATA_FETCH`(05)、`UPLOAD_ASSEMBLE`(06)、`TRANSCODE`(预留未用)。
- **JSONB 与 TEXT[] 列一律不做 JPA 映射。** 计划 03、04 的实体都只映射标量列，`metadata` / `raw_metadata` / `field_sources` / `locked_fields` / `metadata_providers` 全部留空。原因：`ddl-auto=validate` 对 Hibernate 的数组/JSON 类型映射很挑，而这些列天然适合用 `JdbcTemplate` 直读直写。**计划 05 必须沿用这个模式，不要临时改成 `@JdbcTypeCode`。**
- **批量集合操作用 `JdbcTemplate`，实体操作用 JPA。** 计划 04 的 `ImageLibraryRecalculator`（窗口函数重编页码、前缀聚合计数）与 `ImageTreeRelocator`（一条前缀替换 UPDATE 重写子树）是范例。
- **模块依赖方向**：`scan` 不依赖 `video`/`image`（靠 `LibraryContentBuilder` SPI 倒置）；`video` 与 `image` 互不依赖，只共享 `shared` 里的 `NaturalSortKey` 与 `MaterializedPath`（**复用算法，不复用模型**）。由 `ModularityTests` 强制——**强制点是每个模块 `package-info.java` 上的显式 `allowedDependencies`**，只写 `displayName` 等于不设限。`video` 那份是 2026-08-19 补的（`{"shared", "user", "library", "scan", "scan::spi", "scan::events"}`，无 `jobs`），`image` 那份由计划 04 Task 1 写全（带 `jobs`）。
- **命名接口一律叫 `events`（复数）**：嵌套包 `*.event` 上写 `@NamedInterface("events")`，引用方写 `scan::events` / `video::events` / `image::events`；SPI 包是 `@NamedInterface(name = "spi")` → `scan::spi`。名字写错不是断言失败，是 `ApplicationModules` 加载期直接抛异常，整批架构测试一起挂。
- **`LibraryContentBuilder` 有三个回调**：`onFileDiscovered` / `onFileChanged` / `onFileVanished`。`onFileChanged` 同时覆盖「内容变了」与「路径从 `MISSING` 恢复」（`reactivated = true`）。视频域可以空实现（语义结构不依赖文件内容），**图片域不行**——CBZ 的页表是上次解包的快照，包被重打就得重排 `ARCHIVE_INDEX`。
- **扫描绝不删除数据**：文件消失只标记 `scanned_file.status = MISSING`，语义层一行不删。所有"回收"逻辑都必须建立在这个前提上。
- **404 而非 403**：无权访问一律抛 `NotFoundException`，不泄露资源存在性。
- **测试里跑任务**：`scanTrigger.requestScan(id)` 后调 `jobPoller.pollOnce()`。两个坑叠在一起：扫描会再排出下游任务（如 `ARCHIVE_INDEX`），而 **`pollOnce()` 是异步提交、同步返回的**——它返回不代表任务跑完了。所以**不要靠数 `pollOnce()` 的次数凑时序**，要用 Awaitility 轮询到 `job` 表里没有 `PENDING`/`RUNNING` 为止。计划 03 执行时就栽在这上面（`146b7e3`、`39396c3` 是补救提交）。而且 `AbstractIntegrationTest` 把 `mymedia.jobs.enabled` 设成了 `false`，`JobPoller` 的唯一实现 `JobScheduler` 又是 `@ConditionalOnProperty(havingValue = "true")`——**默认容器里没有这个 bean**，`@Autowired JobPoller` 会让上下文启动失败。每个要跑任务的测试类都得自己带 `@TestPropertySource(properties = {"mymedia.jobs.enabled=true", "mymedia.jobs.poll-interval=PT1H"})`，后一条是为了关掉后台定时轮询，免得它和手动 `pollOnce()` 抢任务。
- `AbstractIntegrationTest` 不带 `@Transactional`，容器在测试间共享——测试要用 `UUID` 造独立的库名与用户名。
- **`job` 表跨测试共享且不清理**（`dedup_key` 只压 `PENDING`/`RUNNING`，已 `SUCCEEDED` 的行永久累积）：任何「全表 `count(*)` == 固定值」的断言在非首跑位置必然失败。写断言用「按 id 查状态」或「请求前基线 + 增量」；同样的原因，测试故意遗留的 `PENDING` 任务会污染后续测试（指向已删 `@TempDir` 的任务被下一个测试抢到后失败重试、队列永不排空）——**测试结束前必须把队列 drain 干净**（计划 04 执行时踩过，见 §9 P5/P6）。

## 3. 已实测验证的事实（不要凭记忆推翻）

| 事实 | 验证方式 |
|---|---|
| Spring Boot `4.1.0`（不是 `4.1.0.RELEASE`） | Maven Central `maven-metadata.xml`（计划 01） |
| Boot 4 starter 改名表、Testcontainers 2.x 包名 | 实测生成骨架（计划 01） |
| `commons-compress` 最新为 `1.28.0`，传递引入 `commons-io:2.20.0` | 本次：`maven-metadata.xml` + `dependency` |
| `ZipFile.builder().setPath(p).setCharset(cs).get()` 可编译可运行 | 本次：下载 jar → `javap` → 实跑 |
| **`setCharset` 是回退编码不是强制编码**：条目打了 ZIP 通用位第 11 位就按 UTF-8 解，配置的 charset 完全不参与 | 本次：同一归档分别用 UTF-8 与 GBK 打开，条目名字节完全相同（`第1章/001.jpg` → `e7acac31e7aba02f…`），`usesUTF8ForNames = true` |
| 因此把回退编码设成 `GBK` **不会**破坏 UTF-8 归档 | 同上 |
| Bangumi 无需鉴权：`GET /v0/subjects/{id}` → 200 | 本次实跑 |
| Bangumi 搜索：`POST /v0/search/subjects?limit=N`，体 `{"keyword":"…","filter":{"type":[2]}}` → 200，`进击的巨人` 命中 32 条 | 本次实跑 |
| 搜索结果字段：`id, name, name_cn, date, rating.score, image, images, eps, infobox, meta_tags, platform, collection, locked, nsfw`（**没有 summary**） | 本次实跑 |
| 详情多出：`summary, tags, type, total_episodes, volumes, series` | 本次实跑 `/v0/subjects/55770` |
| Bangumi `type`：1=书籍 2=动画 3=音乐 4=游戏 6=三次元 | 同上 |
| JDK 25 下 `com.sun.net.httpserver.HttpServer` 可从 classpath 直接用 | 本次实跑 |
| `pom.xml` 里**没有** `commons-compress`（计划 02 只在约束表里提了一句没真加） | 本次核对；已在计划 04 Task 4 补上 |
| `MediaLibrary` 实体**没有映射** `metadata_providers` 列（V3 里有这一列） | 本次核对；计划 05 需要它 |
| **Boot 4.1 用的是 Jackson 3**：`tools.jackson.databind.ObjectMapper` / `JsonNode`；`JacksonException` 继承 `RuntimeException`（**不受检**）；`asText()` → `asString()`；`fields()` → `properties()`。注解仍在 `com.fasterxml.jackson.annotation` | 写计划 05 时：`unzip -l target/mymedia-0.0.1-SNAPSHOT.jar` 见 `jackson-databind-3.1.4.jar`，`javap` 逐个方法核对 |
| **Spring Modulith 把模块的嵌套包当作内部实现**：`scan.spi` / `scan.event` / `video.event` / `image.event` 必须加 `@NamedInterface`，引用方 `allowedDependencies` 里写 `scan::spi` 这样的形式，否则 `verify()` 失败 | 写计划 05 时核对计划 01 已落地的显式 `allowedDependencies` 与计划 02–04 的嵌套包用法 |
| **`'进击的巨人' % '巨人'` 是 `false`**（`similarity` 只有 `0.125`，低于默认阈值 `0.3`）——`%` 操作符**不能**当中文子串搜索的匹配谓词 | 写计划 06 时：`postgres:17` 容器实跑 |
| **查询串少于 3 个字符时 GIN trgm 索引不提供任何过滤**：10 万行表上 `ILIKE '%巨人%'` 索引扫描返回全部 20007 行、耗时 29 ms，与强制顺序扫描的 28.6 ms 持平；三字的 `'%进击的%'` 只要 0.21 ms | 同上，`EXPLAIN ANALYZE` |
| **`to_tsvector('english', …)` 对拉丁文有真实价值**：查 `movies` 命中 `Sintel the Movie`（`ILIKE` 命中 0 行）。两条搜索路径各管各的，不是冗余 | 同上 |
| **生成列必须用双参数的 `to_tsvector(regconfig, text)`**：单参数版本是 STABLE 的，PostgreSQL 会报 `generation expression is not immutable` | 同上 |
| **命名接口的实际名字是 `events`**：`scan/event` 与 `video/event` 的 `package-info.java` 上都是 `@NamedInterface("events")`；Modulith 报错文本里也写作 `scan :: events` | 2026-08-19：`cat` 两个 package-info + 故意把 `video` 的 `allowedDependencies` 收成 `{"shared"}` 跑 `ModularityTests`，看它报的路径名 |
| **`allowedDependencies` 是真的会拦人的**：把 `video` 收成 `{"shared"}` 后 `ModularityTests` 立刻报出 11 条越界依赖（`library`、`scan`、`scan :: spi`、`scan :: events`） | 同上，RED/GREEN 都跑过 |
| **`video` 模块不依赖 `jobs`**：视频域不排任何任务 | 2026-08-19：`grep -r jobs src/main/java/com/mymedia/video/` 无命中，且收紧后 `ModularityTests` 通过 |
| **`job` 的 `dedup_key` 唯一索引只覆盖 `PENDING`/`RUNNING`**（`V4__jobs.sql:23` 的部分唯一索引 + `JobQueue.enqueue` 的 `ON CONFLICT ... WHERE`） | 2026-08-19 读源；这条决定了「同一个压缩包在上一轮任务 SUCCEEDED 之后可以再排一次索引」 |
| **`ScannedFileChanged` 不带 `MediaKind`**（字段只有 `scannedFileId, libraryId, relativePath, sizeBytes, mtime, reactivated`），要区分散图与压缩包得自己查库 | 2026-08-19 读源 |
| **Boot 4 的 multipart 属性没有改名**（仍是 `spring.servlet.multipart.*`，声明在 `spring-boot-servlet-4.1.0.jar`，经 `starter-webmvc → spring-boot-webmvc → spring-boot-servlet` 传递引入，`compile` 作用域，**不需要新增依赖**），但默认上限是 `max-file-size=1MB` / `max-request-size=10MB` | 写计划 06 时：`mvn dependency:tree` |

## 4. 计划 05 的设计定稿（**已落进 `2026-08-17-05-preview-metadata.md`**）

> 本节已完成使命：计划 05 按它写完了，12 个任务与这里的切分一一对应。
> 保留它是为了留下"设计先定稿、再展开成计划"的痕迹；
> **执行时以计划 05 为准**，那份文件里的细节比这里多得多，
> 且修正了本节的两处（见下方 ⚠）。

### 4.1 任务切分（12 个）

1. `derived_asset` 表 + 派生资源存储布局（V10，含给 `cover_asset_id` 补外键）
2. 外部进程运行器 `CommandRunner` + ffprobe 输出解析（纯单元测试）
3. 视频预览生成：`PREVIEW_GENERATE` 处理器（探测回填 `video_file` → 抽帧封面 → 缩略图）
4. 图片预览生成：图集/漫画封面（含 CBZ 首页读取）
5. 雪碧图与 WebVTT（`SPRITE_GENERATE`）
6. 事件接线 + `GET /api/assets/{id}` 资源访问端点 + 封面写回
7. 字段合并策略（`shared`）+ 领域写回入口 + 用户编辑与字段锁定端点
8. `MetadataProvider` SPI + `LocalNfoProvider`
9. `FilenameProvider` + 提供者链编排 `MetadataResolver`
10. 外部 Provider：`BangumiProvider` + `TmdbProvider`（限流、缓存、缺 key 降级）
11. `METADATA_FETCH` 任务 + `scrape_candidate` 候选队列与确认端点 + `collection` 填充
12. 全量验证与讲解文档

### 4.2 模块依赖方向（关键，决定了整个模块设计）

```
preview  → scan, library, jobs, shared, video, image
metadata → scan, library, jobs, shared, video, image
video / image → 绝不引用 preview / metadata
```

无环，`ApplicationModules.verify()` 能过。做法是 `preview` / `metadata` **订阅** `VideoItemCreated` / `ImageNodeCreated`（spec §4.3 的事件表），并**直接调用**领域模块的公开写回 API；领域模块永远不反向引用它们。

**为什么这里不像 `scan` 那样做 SPI 倒置**（这是 ADR 素材）：物理扫描是真正与领域无关的，倒置能让"加第三个域不改扫描代码"成立；而**刮削本身就是领域特定的**（TMDB 管电影、Bangumi 管番剧与漫画），倒置只会把 if/else 换个地方摆，还多一层间接。同一个项目里两次相反的取舍，各有各的理由——比"我全都倒置了"更能说明判断力。

### 4.3 元数据优先级：两套机制各管一件事

spec §7.2 的 `用户编辑 > 本地元数据文件 > 刮削 > 文件名`，用两个既有机制表达，**不要再引入第三套 tier 比较**：

- **链的顺序表达优先级**：`LocalNfo → 各刮削器（按 `libraries.metadata_providers` 配置序） → Filename 兜底`，**命中即停**。
- **`locked_fields` 表达覆盖保护**：用户编辑过的字段写入 `locked_fields`，任何刮削不得覆盖。
- `field_sources` 只记录"这个字段是谁写的"，供界面展示，不参与判定。

`shared` 新增：`MetadataPatch(source, sourceId, fields, extras, rawResponse)`、`MetadataFields`（标准字段名常量）、`FieldMergePolicy`（跳过 `locked_fields`、跳过空值，约 20 行 + 单元测试）。

写回入口（本计划新增到既有模块，属于 Modify）：
- `VideoCatalogService.applyMetadata(itemId, MetadataPatch)` / `applyUserEdit(...)` / `assignCover(itemId, assetId)`
- `ImageCatalogService.applyMetadata(nodeId, MetadataPatch)` / `applyUserEdit(...)` / `assignCover(nodeId, assetId)`
- `ImageCatalogService.openPageForProcessing(fileId)` —— **不做权限校验**，仅供后台任务取首页生成封面，javadoc 必须写明
- `LibraryService.metadataProvidersOf(libraryId)` / `setMetadataProviders(...)` —— 走 `JdbcTemplate` 读写 `text[]`
- 各域的 `*MetadataStore`（package-private，`JdbcTemplate`）读写 `metadata` / `raw_metadata` / `field_sources` / `locked_fields`

`image_node` 没有 `release_date` / `rating` 列，这两个标准字段对图片域落进 `metadata` jsonb。

### 4.4 派生资源

- `derived_asset(id, kind, source_scanned_file_id, relative_path, width, height, size_bytes, generated_at)`，`UNIQUE (source_scanned_file_id, kind)`。
- 存储根由 `mymedia.preview.root` 配置（默认 `./data/derived`），路径两级分片（`covers/{id % 100}/{id}-cover.jpg`），避免单目录堆几十万文件。
  ⚠ 计划 05 里明确了这个 `id` 是**来源文件 id** 而不是 `derived_asset.id`：分片键必须在写文件之前就知道，而后者要等插入之后才有。
- **`cover_asset_id` 的外键在 V10 才加**（`ON DELETE SET NULL`）：V6 / V8 建表时 `derived_asset` 还不存在，所以只有列没有约束。这个顺序本身就是一个讲得清的点。
- 派生目录删光后可由任务队列全量重建，不碰任何用户数据（spec §6.2 的承诺）。

### 4.5 ffmpeg / ffprobe

烘焙进应用镜像，**不要求本机安装**，因此：

- 所有解析逻辑（ffprobe JSON → `video_file` 字段）是**纯单元测试**，喂 fixture 字符串。
- 进程调用走可注入的 `CommandRunner`（`run(List<String> command, Duration timeout) → CommandResult(exitCode, stdout, stderr)`）。
- 任务处理器的集成测试用 Spring `@TestConfiguration` + `@Primary` 提供**桩 runner**（按命令行末尾的输出路径写几个假字节），**不要用 Mockito**——计划 01 把 Boot 4 的 test starter 拆开引了，Mockito 是否在 classpath 上没有验证过。
- 真机调用只保留一个带 `assumeTrue(ffprobe 可用)` 的测试。

命令：

| 用途 | 命令 |
|---|---|
| 探测 | `ffprobe -v quiet -print_format json -show_format -show_streams <file>` |
| 封面 | `ffmpeg -y -ss <duration*0.1> -i <file> -frames:v 1 -vf scale=640:-2 -q:v 3 <out.jpg>`（`-ss` 放 `-i` 前是快速定位） |
| 缩略图 | 不再解一次视频，用 ImageIO 从封面缩到 320 宽 |
| 雪碧图 | `ffmpeg -y -i <file> -vf fps=<100/duration>,scale=160:-2,tile=10x10 -frames:v 1 <out.jpg>` |

**雪碧图固定 100 帧、单张 10×10**，间隔按时长算。这样永远只有一张图、一个 VTT，省掉多图分页的全部复杂度。VTT 在 Java 里按几何关系生成。

探测必须先于抽帧：不知道时长就选不出抽帧点，所以两者在同一个 `PREVIEW_GENERATE` 处理器里顺序执行（spec 的 job type 清单里本来也没有单独的 PROBE 类型）。

### 4.6 刮削

- `MetadataProvider`（public SPI）：`name()`、`supports(LibraryDomain)`、`search(ScrapeSubject) → List<MetadataCandidate>`、`fetch(subject, candidate) → Optional<MetadataPatch>`。
  ⚠ 计划 05 加了 `default boolean available()`：TMDB 缺 key 时靠它安静退出链。
- 置信度：`TitleSimilarity` 用**字符二元组 Dice 系数**（对中文有效，和 `pg_trgm` 的三元组正好是一组可对照的讲解素材）。高分（≥0.8）自动应用 → `MATCHED`；中分（0.4–0.8）写 `scrape_candidate` → `NEEDS_REVIEW`；无候选 → 回落 `FilenameProvider` 并置 `NO_MATCH`（**安静回落，界面不显示为错误**）；网络故障/限流才置 `ERROR` 并重试。
- `libraries.metadata_providers` 为空数组 → 该库条目 `scrape_status = NOT_APPLICABLE`，**根本不排任务**。
- 外部 Provider 必须带标识性 `User-Agent`、客户端侧限流、结果缓存（用 Spring 自带的 `ConcurrentMapCacheManager`，**不引 Redis 也不引 Caffeine**）。
- TMDB 缺 API key 时优雅降级：Provider 直接报告不可用，链跳过它，不算错误。
- **Provider 测试用 JDK 的 `com.sun.net.httpserver.HttpServer` 起本地桩**（已验证可用），不引 MockWebServer，也不赌 `MockRestServiceServer` 对 `RestClient` 的支持。
- `collection` 填充：`MetadataPatch.extras` 带 `collection` 键时，`video` 侧 find-or-create 同库的 `collection` 行并挂上条目（计划 03 只建了表没填）。

## 5. 已知遗留与跨计划缺口

> 这张表是「谁欠谁」的总账。**执行任何一份计划之前先扫一遍属于它的行。**
> 2026-08-19 逐项核对过一次，✅ 表示已经在 `main` 上或已经写进对应计划的正文。

| # | 缺口 | 归谁补 | 状态 |
|---|---|---|---|
| G1 | 计划 02–04 缺 `@NamedInterface`，嵌套包跨模块引用会让 `verify()` 失败 | `scan` 由扫描修复 Task 3 补、`video` 由计划 03 执行时补、`image` 由计划 04 Task 2 Step 3 补 | ✅ 三个都已落地（`image` 于计划 04 Task 2） |
| G2 | `video` / `image` 只写 `displayName`，依赖方向没有强制点 | `video` 直接改代码（2026-08-19）、`image` 由计划 04 Task 1 Step 4 | ✅ 两者都已落地（`image` 于计划 04 Task 1；`ModularityTests` 全量验证在计划 04 Task 10） |
| G3 | 计划 04 的 `ImageContentBuilder` 只实现两个回调，对不上扫描修复后的三回调 SPI，且 CBZ 重打后页表不会重建 | 计划 04 Task 3（实现 + 3 条测试） | ✅ 已执行（Task 3，10/10 测试通过） |
| G4 | `ImageNode.coverAssetId` 是可写映射，与计划 05 的 `JdbcTemplate` 原子回填冲突，陈旧实体会把封面刷回 `null` | 计划 04 Task 1 改成 `insertable = false, updatable = false` 并删掉 `assignCover` | ✅ 已执行（Task 1） |
| G5 | `VideoItem` 没有 `coverAssetId` 映射（列在 V6 里就有） | 计划 06 Task 6 Step 5，同样做成只读 | ✅ 已执行 |
| G6 | `search_vector` 列根本不存在（V6、V8 都漏了，而 spec §7.7 要求它） | 计划 06 Task 1 的 `V12__search_columns.sql`，**必须双参数 `to_tsvector('english', …)`** | ✅ 已执行（本表此前一直漏标，Task 12 交接时顺手订正） |
| G7 | `tag`、`video_item_tag`、`image_node_tag`、`video_favorite`、`image_favorite`、`share_link`、`upload_session`、`upload_chunk` 八张表全未建 | 计划 06 Task 5/7/8/10（V13–V16） | ✅ 全部已建（`tag`/`video_item_tag`/`image_node_tag` V13 Task 5、`video_favorite`/`image_favorite` V14 Task 7、`share_link` V15 Task 8、`upload_session`/`upload_chunk` V16 Task 10） |
| G8 | `SampledHash` 是 `com.mymedia.scan` 的 package-private，而秒传与合并后校验都要用 | 计划 06 Task 10：提升到 `com.mymedia.shared` 并改 public，算法一个字节不动 | ✅ 已执行 |
| G9 | `scanned_file.content_hash` 绝大多数为 `NULL`（计划 02 只在改名检测时才算） | 计划 06 用「同尺寸候选现算」兜住，边界写进 ADR-007；新开 `ScannedFileHashService`，不往查询服务里塞写方法 | ✅ 已执行 |
| G10 | `SecurityConfig` 目前 `anyRequest().authenticated()`，分享链接免登录必须放行 | 计划 06 Task 9 | ✅ 已执行 |
| G11 | `MediaLibrary` 实体没有映射 `metadata_providers` 列（V3 里有这一列） | 计划 05 的 `LibraryService.metadataProvidersOf/setMetadataProviders`，走 `JdbcTemplate` 读写 `text[]` | ⬜ 待执行 |
| G12 | `pom.xml` 里没有 `commons-compress`（计划 02 只在约束表里提了一句没真加） | 计划 04 Task 4 Step 3 | ✅ 已执行（`commons-compress:1.28.0` + 传递 `commons-io:2.20.0` 实测） |
| G13 | `video_group` / `collection` 的封面没有生成逻辑（V10 建了外键但无写入方） | 展示层选择题，用第一个条目的封面代偿即可，留给计划 07 | ✅ 已执行：计划 07 Task 3 按定稿**不给 `VideoGroup` 加映射**，由前端回落到条目封面 |
| G14 | `mymedia.share.secret` 默认为空时每次启动随机生成，重启后带密码的分享链接要访客重新输 | 计划 08 的 README 部署清单写「生产环境配固定值」 | ⬜ |
| G16 | 计划 04 的 7 个测试类 `@Autowired JobPoller` 却没带 `mymedia.jobs.enabled=true`，上下文起不来 | 计划 04：7 个类统一补 `@TestPropertySource` | ✅ 已执行（全部测试类落实，并写进其 Global Constraints） |
| G17 | 计划 04 的 9 处 `scan()` 助手靠「调两次 `pollOnce()`」凑时序，而它是异步的——断言会跑在任务完成之前 | 计划 04：统一换成 Awaitility 轮询到队列排空；Task 3 那条「只跑扫描」的用例单独处理 | ✅ 已执行（执行中又发现一处变体，见 §9） |
| G15 | `.superpowers/sdd/` 只有 01、02 两个 review package，03 的缺 | 无人——计划 03 执行时没留，事后补不出来。记在这里免得下次误以为丢了文件 | — |
| G18 | Provider 缓存当前没有淘汰策略 | 后续接入更多提供者或长期运行后再评估缓存生命周期 | ⬜ |
| G19 | 图片域会为每个 ACTIVE 节点排刮削，包括纯中间目录 | 若实测噪音过大，在 `nodesPendingScrape` 查询中收敛，不在界面层掩盖 | ⬜ |
| G20 | 秒传只在库内生效，同库同尺寸候选现算上限 8 个；超过就落空走正常上传 | 落空的代价只是慢不是错；若命中率实测太低，收敛方向是加一个低优先级 `HASH_BACKFILL` 任务类型，**不要**让扫描每次对账全量算哈希（ADR-007） | ⬜ |
| G21 | 上传的临时目录（`{tempRoot}/{sessionId}/`）没有过期清理，用户建了会话不传会一直占磁盘 | 加一个定期任务扫 `status = 'RECEIVING'` 且 `created_at` 超过 N 天的会话即可，属运维完善，不在 P11 验收范围 | ⬜ |
| G22 | 分享链接访问没有速率限制：令牌本身熵足够抗枚举，但带密码链接的 `/unlock` 端点理论上可被爆破（bcrypt 本身是一层缓冲） | 若要加，正确的位置是按 `token` 的计数器，不是全局限流 | ⬜ |
| G23 | 全局与领域搜索都没有分页，只有 `limit`（上限 200） | 媒体库搜索是"找那一部"不是"翻页浏览"，真需要时按 `(score, id)` 游标分页，不要用 `OFFSET` | ⬜ |
| G24 | `ImageFavoriteService.listNodes`/`VideoFavoriteService.listItems`（计划 06 Task 7）在 `findByIds` 之后不重新校验当前库访问权，库访问被撤销后收藏列表仍会吐出该条目信息；与 `continueWatching`/`continueReading`（G-无编号，见 §9）同一类缺口。计划 06 合并前审查发现，见 §12 | 与 `continueWatching`/`continueReading`/`VideoProgressService.record` 一并改：统一在 catalog 查询后过滤 `accessService.canAccess`，或改成一次 join 查询下推 | 🔶 部分执行：计划 07 Task 3 关掉了四处（两个收藏 + 两个继续列表），每处都带了「撤销访问权后列表为空」的用例。**`VideoProgressService.record` 故意没动**——Task 3 preflight Ruling R3：`VideoProgressServiceTest` 的既有用例建的是不 `grant` 权限的用户，加校验会带崩约 10 个用例，判定为 Task 3 范围外的连锁改动；代价是视频进度仍可对无访问权的库写入（写的是用户自己的进度行，不泄露他人内容）。若要收口留给计划 08 或专门一次小改动 |
| G25 | 带密码的分享链接不支持媒体流播放（计划 07 Task 16 明写的刻意取舍：分享票据走 `X-Share-Ticket` 请求头，`<video>`/`<img>` 同样带不了自定义头；给它开 query 参数入口等于把凭证挪到一个本来就设计给到处转发的 URL 上，密码保护形同虚设） | 若要做，正确形态是让 `VideoShareController`/`ImageShareController` 也接受 `?ticket=` 且把 TTL 压到分钟级，把「转发链接=永久生效」收窄成「转发链接=几分钟内生效」；完整取舍见 ADR-008 | ⬜（刻意不做，非缺陷） |
| G26 | 图片分享里一个节点同时 `readable` 且 `browsable` 时，分享页（计划 07 Task 16）只展示阅读器、不展示浏览视图——分享场景下的二选一被简化处理，登录后在库内浏览没有这个限制 | 若要收口，`ShareView.vue` 需要在两者都为真时加一层选择界面，成本不高，留给需要时再做 | ⬜ |
| G27 | `GET /api/scrape/queue`（计划 07 Task 18 新增）与 `LibraryAdminView.vue` 的刮削器列（计划 07 Task 17）都是逐目标/逐库查询，没有批量端点 | 自托管规模（几个到几十个库/待确认目标）下代价可接受；若库/待确认目标数量级增长，收敛方向是加一个批量查询端点 | ⬜ |
| G28 | 刮削候选队列界面没有候选封面（`MetadataDto.CandidateResponse` 的后端契约本来就没有封面字段，候选搜索结果本身不含图片 URL）——计划 07 Task 18 review 已核实是真实契约限制，不是实现遗漏 | 若要加，需要新增后端字段，或在确认前多打一次候选详情请求，成本收益需要评估 | ⬜ |
| G29 | 上传会话的本地续传记录用单一 `localStorage` key（计划 07 Task 18），同时开第二个上传会覆盖前一个未完成会话的续传信息——不影响服务端数据，只是那次会话失去界面上的断点续传入口 | 若要收口，改成以 sessionId 为 key 的多会话记录，成本不高，留给需要时再做 | ⬜ |
| G30 | 图片域没有分享创建入口，只有视频域的 `ItemDetailView.vue` 有（Task 9 产出），`NodeBrowseView.vue` 没有对称实现；后端 `POST /api/image/nodes/{id}/share` 端点已存在但前端未接（计划 07 whole-branch final review 发现） | 若要补，直接照抄 `ItemDetailView.vue` 的分享面板模式（`createShare('image', nodeId, body)`），留给后续任务 | ⬜ |

### 已经定稿、不再讨论的取舍

- 计划 03 的孤儿条目缺陷**已修复**（Task 5 Step 7 新增 `VideoScanFinalizer`，原 Step 7 顺延为 Step 8，用例数 6 → 7）。修复理由与图片域的对称做法记在计划 04 的 Self-Review 末尾。
- spec §7.4 的「按 Accept 头转 WebP」与「服务端预读下一页」在计划 04 中**明确不做**，理由写在该计划的 Self-Review 里。
- spec §14 待定项里，「多重 Range」已在计划 03 定稿为**返回并集**；「认证形态」在计划 01 定稿；「嵌套 Collection」维持不做。
- **计划 05 与 spec §4.3 有一处定稿偏离**：两个事件监听器用同步 `@TransactionalEventListener(AFTER_COMMIT)` 而不是 Modulith 的异步持久化监听器。理由是它们只排一个**已经持久化**的任务，重试与历史由 `job` 表负责；spec 担心的漏生成由 `LibraryScanCompleted` 上的补齐监听器兜住。
- **接受「两字中文查询走全表扫描」这个上界**（10 万条目 29 ms），理由与退路写进 ADR-006。

### 计划 06 的四个拍板决策（写计划时定的，执行时别再翻案）

| 决策点 | 结论 |
|---|---|
| 搜索与标签放哪个模块 | **不新开自造模块**。各领域模块提供自己的搜索与收藏；标签归 `metadata`（它已有 video/image 两条边）；全局搜索放进 spec §4.2 早已声明、计划 06 才创建的 `web` 模块 |
| 秒传如何处理 `content_hash` 大面积为空 | **命中即秒传 + 同尺寸候选现算**：先按 hash 查；未命中时取同库内 `size_bytes` 相同且 hash 为空的候选（上限 8 个）现算并写回再比 |
| 分享链接的免登录访问 | **独立 `/api/share/{token}/**` 前缀**，`permitAll`；令牌解析成临时只读授权（`ShareGrant`），复用既有的 `VideoStreamService` / `ImagePageService`，不复制播放与阅读逻辑 |
| 计划 06 是否拆分 | **不拆**，P10（Task 1–9）在前 P11（Task 10–13）在后，两段之间是一个可独立验收的分界点 |

### 计划 06 执行时依赖上游落地形态的三处

计划里都写了「若不同该怎么调整」，执行到那儿对一下就行：
`derived_asset.source_scanned_file_id` 是否可空（Task 6 的最后一个用例）——
**已核实为 `NOT NULL`，Task 6 执行时按计划自带的兜底方案调整（先插一行 `scanned_file`），已解决**；
`image_node` 建树时物化路径的回填方式（Task 9 的 `insertNode` 助手）、
`MyMediaApplication` 上的 `@ConfigurationPropertiesScan`（计划 05 Task 1 Step 8 已加）。

## 6. 决策记录（ADR）

| 编号 | 主题 | 由谁产出 |
|---|---|---|
| ADR-001 | 域分区的数据库级强制 | 已存在 |
| ADR-002 | 认证方案 | 已存在 |
| ADR-003 | 用数据库任务表替代消息队列 | 已存在 |
| ADR-004 | 刮削链不做 SPI 倒置 | 计划 05 Task 12 |
| ADR-005 | 刮削是可选增强 | 计划 05 Task 12 |
| ADR-006 | 中文搜索的真实边界 | 计划 06 Task 13 |
| ADR-007 | 采样哈希与秒传的边界 | 计划 06 Task 13 |
| ADR-008 | 媒体票据（URL 携带凭证的边界） | 计划 07 Task 19 |

**009 起可用。**

## 7. 计划 03 执行交接（2026-08-19）

- **状态**：已完成 Task 1–10，合入 `main`，并创建 `plan-03-video_domain` tag。
- **核心产物**：`video_item` / `video_group` / `video_file` / `video_folder` / `video_progress`、文件名解析、目录树浏览、完整 HTTP Range 流式播放、进度与继续观看、条目目录 API。
- **验证**：在合并后的 `main` 上运行 `mvn -B -ntp clean verify`，`226` tests，`0` failures，`0` errors，`1` skipped，`BUILD SUCCESS`。
- **测试约定**：需要手动触发扫描的集成测试显式启用 `JobPoller`，并等待 `JobStatus.SUCCEEDED`；`StreamingResponseBody` 的 MockMvc 测试先等待异步生命周期完成，再执行 `asyncDispatch`。
- **下一步**：新窗口先读本文件，再读 `2026-08-17-04-image-domain.md`；从 `main` 创建计划 04 的独立 worktree，迁移编号从 V8 开始。

## 8. 计划 04 执行前复核（2026-08-19）

计划 03 执行完之后做了一次「计划到目前为止对不对得上代码」的复核。发现的问题**都已改掉**，
这里留一份账，免得下次又当成新发现。

**改了代码的一处**

- `src/main/java/com/mymedia/video/package-info.java` 补上显式 `allowedDependencies`。
  在此之前它只有 `displayName`，等于对 `video` 不设限——§2 那句「由 `ModularityTests` 强制」
  当时是假的。验证：先把它收成 `{"shared"}` 跑出 11 条越界（RED），再改成实际集合跑通（GREEN），
  `ModularityTests` 2 tests 0 failures。

**改了计划文本的四处**

| 改哪 | 为什么 |
|---|---|
| 计划 04 Task 3 补 `onFileChanged`（实现 + 3 条测试） | 扫描修复给 SPI 加了第三个回调，不实现直接编译不过；且 CBZ 重打后必须重排 `ARCHIVE_INDEX` |
| 计划 04 Task 1/2 补 `image` 的 `allowedDependencies` 与 `image.event` 的 `@NamedInterface("events")` | 否则计划 05 订阅 `ImageNodeCreated` 时 `verify()` 失败 |
| 计划 04 Task 1 的 `ImageNode.coverAssetId` 改只读、删 `assignCover` | 与计划 05 的 `JdbcTemplate` 原子回填冲突，陈旧实体会把封面刷回 `null` |
| 计划 05 全文 `::event` → `::events`，Task 6 Step 1/2 重写 | 计划 05 写的是单数，`main` 上落地的是复数；名字对不上是**加载期异常**不是断言失败 |
| 计划 04 的 7 个测试类补 `@TestPropertySource(mymedia.jobs.enabled=true, poll-interval=PT1H)` | `AbstractIntegrationTest` 把 jobs 关了、`JobScheduler` 又是条件 bean，`@Autowired JobPoller` 会让上下文直接起不来——**一条断言都跑不到**。计划 03 的七个测试类是带这段的，计划 04 写的时候漏了 |
| 计划 04 的 9 处 `scan()` 助手改成 Awaitility 轮询到队列排空 | `pollOnce()` 异步提交、同步返回，「调两次就等于都跑完了」是错的。计划 03 执行时踩过同一个坑并靠 Awaitility 补救，计划 04 沿用了补救之前的写法 |

**改了文档状态的两处**

- `2026-08-18-scanning-important-fixes.md` 加了「已执行」横幅并勾上 23 个 step。
  它此前 0 个勾、且没被任何索引文件提到，看起来像还没做。
- `STATUS.md` 并入本文件后删除。它当时还写着「下一步执行计划 02」，落后两个计划，
  而它自称是「跨会话第一入口」。

**没动、但下一个窗口要知道的**

1. `.superpowers/sdd/` 缺计划 03 的 review package（见 G15），事后补不出来。
2. 计划 07（前端）与 08（交付）仍未写。07 需要先定前端技术栈，**这是要用户拍板的决策点，不要自己定**。
3. 本次复核在改完之后跑过全量 `mvn -B -ntp clean verify`：
   **`226` tests，`0` failures，`0` errors，`1` skipped，`BUILD SUCCESS`**——
   与计划 03 交付时的数字一致，收紧 `video` 的依赖边界没有动到任何行为。
4. 计划 03 的 worktree `D:/MyMedia-3` 与分支 `plan03-2026-08-17-03-video-domain`
   已删除（内容全部在 `main` 上，tag `plan-03-video_domain` 仍在）。

## 9. 计划 04 执行交接（2026-08-21 执行，2026-08-22 完成并合入）

- **状态**：✅ **Task 1–10 全部完成，已合入 `main` 并推送**（合并提交 `41cad79`，
  tag `plan-04-image_domain`）。合并前跑过 clean verify：**`313` tests，`0` failures，
  `0` errors，`1` skipped，`BUILD SUCCESS`**（计划 03 交付时是 226）。
- **提交**（Task 1–6，8 个）：`b42ef5f` Task 1 表与实体 → `78c7f7a` Task 2 索引器 →
  `9f71ac7` Task 3 语义层 → `853833e` Task 4 CBZ 页索引 → `c443369` 孤儿任务测试修复 →
  `a373e93` Task 5 扫描收尾 → `040fa0b` Task 6 目录改名移动 → `bedc901` 补末尾换行。
- **提交**（Task 7–10，10 个）：`7215d38` Task 7 浏览 API 与阅读模式覆盖 →
  `0efa098` 浏览端点补库访问权校验 → `a0a4dcd` Task 8 分页阅读与单页流式输出 →
  `8b51778` 打开失败留痕 → `68c87b7` Task 9 阅读进度与继续阅读（V9）→
  `e7281dd` 进度记录补库访问权校验 → `d790a11` 压缩包索引完成后刷新节点计数 →
  `85c891f` 归档索引计数基线化 → `2fdfdf8` Task 10 收尾（讲解文档 + README）→
  `5e4cf9e` 压缩包与同名目录并存时跳过建索引。合并前 review 又补了 `19d5a6c`（见下表 P7）。
- **测试**：Task 1: 10/10 · Task 2: 8/8 · Task 3: 10/10（Task 4 落地后全绿）·
  Task 4: 12/12 · Task 5: 8/8 · Task 6: 15/15 · 合并前全量 313/313。
  集成测试走 Testcontainers `postgres:17`。
- **执行中发现并已修正的计划缺陷**（全部经用户确认后落地，执行时以此为正确版本）：

| # | 缺陷 | 修正 |
|---|---|---|
| P1 | Task 4 `ArchiveIndexJobHandler` 的 import 写的是 Jackson 2（`com.fasterxml.databind`），classpath 只有 Jackson 3（`tools.jackson.databind:3.1.4`），照写编译不过 | 换成 `tools.jackson.databind`，与 `LibraryScanJobHandler` 一致 |
| P2 | Task 6 `ImageTreeRelocator.tryMoveWholeDirectory` 只搬组的最底层节点：顶层目录整体改名时（测试 `movingTheParentAlsoCarriesNestedDirectories`）旧祖先会被当空壳回收，`getNode(旧中层)` 抛 NotFound | 整组搬走判据成立后逐层上溯——祖先整棵子树都搬走（子树页数 == 本组移动数）就继续，从最顶层候选往下尝试，目标被真实内容占据则退下一层；全失败回落逐文件重挂。7 条测试全过 |
| P3 | Task 8 `ImagePageControllerTest` 两条断言 body 的用例没有 `asyncDispatch`，`StreamingResponseBody` 的 body 在异步派发前为空 | ✅ 已应用（Task 8 用 `asyncStarted + getAsyncResult + asyncDispatch` 模式） |
| P4 | Task 3 的预期测试数「7」是 2026-08-19 补 3 条 `onFileChanged` 用例前的数字，且其中 2 条（`changedArchiveIsReIndexed`、`reactivatedArchiveIsReIndexedWithoutDuplicatingPages`）依赖 Task 4 的真实 handler | 以 8/10 绿 + 2 条预期红提交，Task 4 落地后全绿（已实测 10/10） |
| P5 | Task 3 `archiveBecomesALeafNodeAndSchedulesAnIndexJob` 的 `count(LIBRARY_SCAN SUCCEEDED)==1` 在共享容器下不可能成立：job 表跨测试累积、`dedup_key` 只压 PENDING/RUNNING | 改用 `requestScan` 返回的 jobId 查状态 + `ARCHIVE_INDEX` 计数基线化（before+1） |
| P6 | Task 3 同一条用例故意遗留 PENDING 的 `ARCHIVE_INDEX` 指向已删除的 `@TempDir`；Task 4 换真 handler 后，下一个测试抢到孤儿任务→失败→30s 退避重试→`pendingOrRunningJobs()` 永不归零→后续 5 个测试超时 | 该用例断言后 drain 队列（保持断言前只轮询一次的意图） |
| P7 | **合并前 review 发现**：`ImageTreeRelocator.tryMoveWholeDirectory` 用同一个 `topLevel + i` 同时切旧路径和新路径，隐含假设两者深度相同。目录被**上移**（`作者A/系列B` → `系列B`）时新路径更浅，`subList` 越界抛 `IndexOutOfBoundsException`；异常从 `ImageScanFinalizer` 冒出去，`recalculate` 不再执行，扫描任务本身也失败后反复退避重试，队列永不排空 | 改为从底部基候选倒着对齐（`newCount = newSegments.size() - oldSegments.size() + topLevel + i`），新路径里没有对应段的祖先跳过不改名、空了由重算回收；补回归测试 `movingANestedDirectoryUpToTheRootKeepsItsNode`。提交 `19d5a6c` |

- **已确认、暂缓的 Minor**（不阻塞，最终 review 时再清）：新文件缺末尾换行（仅 Task 6 的已补）；`ImageNodeCreated` 事件发布无测试断言；同名防撞/库根压缩包/同名异父无测试；`recalculationSkipsVideoLibraries` 无法检测域守卫被删（brief 固有弱点）；两级空目录级联回收无测试；finalizer 对已删库事件会 NPE（窗口极小）；`ArchivePage.sizeBytes` 暂无读取方（计划预留）。
- **SDD 工作区**：`.superpowers/sdd/2026-08-17-04-image-domain/`（git-ignored）有 ledger、各任务 brief/report/review package。ledger 含全部 Ruling 明细。
- **合并时的 review 结论**：模块边界（`ModularityTests` 绿、`grep com.mymedia.image src/main/java/com/mymedia/scan/` 无输出）、访问权校验（浏览 / 单页 / 节点 / 进度四个控制器一律 404 不泄露存在性）、压缩包资源管理（流关闭时一并关 `ZipFile`，Windows 上不会锁住文件）、迁移与实体一致性都已逐项确认。唯一拦下来的是上面的 P7，已修复后再合并。
- **合并后遗留的 Minor**（不阻塞，可在计划 05–06 顺手清）：
  1. `continueReading` / `continueWatching` 都不按库访问权过滤——用户被撤销授权后，历史进度里的 id 仍会列出来。两个域行为一致，要改就一起改。
  2. `VideoProgressService.record` 没有库访问权校验（图片域这次补了，视频域是计划 03 的既有缺口）。
  3. `README.md` 的模块图与「五个模块」表述仍停在计划 01（缺 `video` / `image`），计划 08 交付时统一重写。
- **当时的下一步**：新窗口先读本文件，再读 `2026-08-17-05-preview-metadata.md`；从 `main` 创建计划 05 的独立 worktree，迁移编号从 V10 开始。计划 04 的 worktree `D:\MyMedia-04` 与分支 `plan04-2026-08-17-04-image-domain` 可以删了（内容全部在 `main` 上，tag `plan-04-image_domain` 仍在）。当前进度见 §10。

## 10. 计划 05 执行交接（2026-08-24）

- **工作区与状态**：`D:\MyMedia-5`，分支 `plan05-2026-08-17-05-preview-metadata`；Task 1–11 已提交。Task 6 主实现提交为 `9ce2a86`，review 修复提交为 `1cd96d5`；Task 7 提交为 `0ba05a6`、`05da9f3`，Task 8 提交为 `4f9f24a`，Task 9 提交为 `6e00ea0`，Task 10 提交链为 `db0d7fd`、`227f96c`、`d0a8f5a`、`eca2822`，Task 11 提交为 `2bcf96d`、`37ec412`。未修改 `main`。
- **Task 1–5 提交链**：`a0f2ed4`（derived asset 存储）→ `b429883`（CommandRunner 与 ffprobe）→ `f242256`（视频预览）→ `e191bb4`（图片预览）→ `7328a2b`（雪碧图与 WebVTT）。
- **Task 6 主实现**：扫描入口增加事务边界；`VideoItemCreated` / `ImageNodeCreated` 在 `AFTER_COMMIT` 后排预览；`LibraryScanCompleted` 补排缺失预览；`GET /api/assets/{id}` 沿来源文件和媒体库做权限校验，未知或无权统一 404，匿名请求 401，并提供 ETag、304、缓存头和流式响应。
- **Task 6 review 修复**：`PreviewBackfill` 改按 active `video_file` 且缺少 `COVER` 派生资源的文件查询，SQL 层限制单轮最多 500 个，覆盖已有封面条目新增文件；资源端点在 ETag 判断前检查物理文件；补充对应回归测试、`ImageNodeCreated` 接线测试和空归档 `attempts == 1` 断言；`application.yml` 说明 `mymedia.preview.wiring-enabled`，删除重复的临时 Task 6 计划文件。已提交 `1cd96d5`。
- **Task 6 测试验证**：`mvn -B -ntp test "-Dtest=PreviewWiringTest,AssetControllerTest,ModularityTests,VideoPreviewJobTest,ImagePreviewJobTest,SpriteJobTest" "-DfailIfNoTests=false"`：37 tests，0 failures，0 errors，0 skipped，`BUILD SUCCESS`；Task 11 完成后再次执行的 focused 套件仍为 37/37。此前全量 `mvn -B -ntp test` 为 378 tests，0 failures，0 errors，1 skipped，`BUILD SUCCESS`。跳过项是既有 Windows 符号链接权限假设，不是 Task 6 回归。`mvn -B -ntp -DskipTests compile` 与 `git diff --check` 也通过。
- **环境记录**：第一次 focused 执行因 Docker Desktop 未启动导致 Testcontainers 无法连接；启动 Docker Desktop 后按原命令重跑通过。不要把第一次环境失败误判为代码失败。
- **独立审查**：未发现 Critical/Important 问题。残余 Minor：补齐始终从 id 最小的 500 个文件开始，持续失败时可能饥饿；仅删物理文件而保留数据库行不会触发补齐；毫秒级 ETag 可能碰撞；流读取异常目前静默吞掉；500 上限测试主要观察 `PENDING`，旧测试仍有少量全局队列等待。均不阻塞继续 Task 7。
- **Task 7 实现**：新增 `shared` 元数据契约与 `FieldMergePolicy`；视频/图片域新增 package-private `JdbcTemplate` 元数据存储及公开写回入口；新增 `metadata` 模块、用户编辑端点和媒体库刮削器配置端点。用户编辑字段写入 `locked_fields`，刮削跳过锁定字段与空值；图片域的 `releaseDate` / `rating` / `originalTitle` 保存在 `metadata` JSONB。Task 7 review 后补上 XML 原始响应 JSONB 安全存储、用户编辑与刮削的 PostgreSQL 行锁串行化、未知媒体库 404。
- **Task 7 测试与审查**：`mvn -B -ntp test "-Dtest=FieldMergePolicyTest,MetadataWriteBackTest,ModularityTests" "-DfailIfNoTests=false"`：22 tests，0 failures，0 errors，`BUILD SUCCESS`；`mvn -B -ntp test`：398 tests，0 failures，0 errors，1 skipped，`BUILD SUCCESS`。唯一跳过项仍是 Windows 符号链接权限限制。任务级 review 与 scoped re-review 均通过，无未解决 Critical/Important；Task 7 遗留 Minor 为排序键断言偏弱、图片元数据和 provider GET 端点测试覆盖不足。
- **Task 7 裁决**：Task 7 正文明确要求 `LibraryService` 直接注入 `JdbcTemplate` 并实现 `metadataProvidersOf/setMetadataProviders`，因此未额外拆出 package-private store；该裁决及代价已记入 SDD ledger。
- **Task 8 实现**：新增 `MetadataProvider` SPI、`ScrapeSubject`、`MetadataCandidate`、`ProviderUnavailableException`；`NfoParser` 支持 Kodi/Jellyfin `.nfo` XML 与项目 `metadata.json`，XML 禁用 DTD、外部实体和 XInclude；`LocalNfoProvider` 按固定顺序查找同名 `.nfo`、`movie.nfo`、`tvshow.nfo`、`metadata.json`，坏文件安静跳过并交给后续提供者。
- **Task 8 测试与审查**：`mvn -B -ntp test "-Dtest=NfoParserTest,LocalNfoProviderTest" "-DfailIfNoTests=false"`：18 tests，0 failures，0 errors，0 skipped，`BUILD SUCCESS`；实现代理报告全量测试 416 passed、1 skipped，唯一跳过项仍是 Windows 符号链接权限限制。任务级规格与质量审查通过，无未解决问题。
- **Task 9 实现与验证**：新增字符二元组 Dice 相似度、文件名兜底、`MetadataProperties`、`ResolutionResult` 与 `MetadataResolver`；链顺序固定为 `LocalNfo → 配置提供者 → Filename`，按 `0.8` / `0.4` 阈值区分自动应用、候选确认与安静回落，提供者故障可继续尝试后续提供者。focused 测试 19/19 通过；提交前全量测试 435 passed、1 skipped，任务级规格与质量审查通过，无未解决问题。
- **Task 10 实现与验证**：新增 `HttpProviderSupport`、`ConcurrentMapCacheManager`、Bangumi/TMDB 提供者与 JDK HTTP 桩；统一 UA、provider 级限流、有限连接/读取超时及 404/不可用状态语义，TMDB 缺 key 安静降级，Bangumi 搜索缓存按标题与年份隔离。focused provider 测试 21/21 通过；完整测试 456 passed、1 skipped；任务级审查与三轮修复 scoped re-review 通过。TMDB 未做真机验证，按计划仅以本地桩覆盖。
- **Task 11 实现与验证**：新增 V11 `scrape_candidate` 表、候选读写/确认/忽略端点、`METADATA_FETCH` 任务、扫描事件接线、合集填充与视频/图片待刮削查询；补充 Kodi `<set>` 和 TMDB `belongs_to_collection`。focused `MetadataFetchJobTest,ModularityTests` 为 15 tests，0 failures，0 errors，0 skipped，`BUILD SUCCESS`；Task 11 完成后全量 `mvn -B -ntp test` 为 469 tests，0 failures，0 errors，1 skipped，`BUILD SUCCESS`。任务级审查发现的 3 个 Important 已修复并通过 scoped re-review；遗留一个未覆盖并发 find-or-create 的 Minor。TMDB 仍仅按桩测试，未做真机验证。
- **Task 12 已完成（2026-08-24）**：新增唯一的真机 ffprobe smoke test（本机无 ffprobe 时跳过）、ADR-004/005 和 `docs/walkthrough/05-预览与元数据.md`；当前工作区执行 `mvn -B -ntp verify` 为 **471 tests，0 failures，0 errors，2 skipped，BUILD SUCCESS**，并由 `FlywayMigrationTest` 的 3 个测试、4 个断言确认 V10/V11 与两张计划 05 表存在。两个 skipped 分别是本机无 ffprobe 和既有 Windows 符号链接权限限制。TMDB 仍仅按本地桩测试，未做真机验证；保留 `mymedia.preview.wiring-enabled` 的测试隔离约定，禁止修改已执行的 V10；元数据 JSONB、`TEXT[]` 继续由 `JdbcTemplate` 读写。
- **最终审查与合并（2026-08-24）**：合并前在 `D:\MyMedia-5` 跑 `mvn -B -ntp clean verify`，
  **471 tests，0 failures，0 errors，2 skipped，BUILD SUCCESS**。逐项复核了迁移与外键、
  模块边界、四个新端点的访问权校验、外部进程与 HTTP 边界、事务与并发写入之后，只改了三处
  （提交 `d81cecf`）：
  1. 删掉 `VideoCatalogService.itemsWithoutCover`——Task 6 的 review 已把视频侧补齐改成按
     `video_file` 查询，这个按条目查的方法自那时起就没有调用方了；顺带在 `PreviewBackfill`
     上写明这条 join 为什么留在 `preview` 而不是下推给 `video`：它连接 `video_file` 与本模块
     自己的 `derived_asset`，只能由**允许依赖对方的那一侧**持有，`preview → video` 是允许的
     方向，反过来不是。
  2. `VideoCatalogService.applyMetadata` 原本对同一行连做两次 `SELECT ... FOR UPDATE`，
     改成取一次，与图片域写法一致。
  3. 补齐 24 个新文件缺失的末尾换行（§9 记的遗留 Minor）。**`V10__derived_asset.sql` 刻意不补**：
     改动已执行迁移会改变 Flyway 校验和。
  改动后重跑 `mvn -B -ntp clean verify` 仍为 471 tests / 0 failures / 0 errors / 2 skipped。
- **审查中查实、确认不是问题的两处**（有实证，别再重复怀疑）：
  - `@Transactional` 标在**包级私有方法**上是有效的。Spring Framework 7 的
    `AbstractTransactionManagementConfiguration` 注册的是
    `new AnnotationTransactionAttributeSource(false)`——`publicMethodsOnly` 为 `false`
    （对 `spring-tx-7.0.8.jar` javap 实证）。旧文档里"非 public 方法上的 @Transactional 会被
    静默忽略"的说法对 Spring 7 已不成立，`LibraryScanner.scan` 的事务边界是真的。
  - TMDB 的 `api_key` **不会**随 I/O 异常进日志：Spring 的 `DefaultRestClient` 构造
    `ResourceAccessException` 消息时在 `?` 处截断 URL（同样 javap 实证）。
- **合并前已知、仍然遗留的 Minor**（不阻塞）：
  1. `applyUserEdit` 收到非标准字段名（比如 `director`）时，值不会落进任何列或 jsonb，
     却仍会写进 `field_sources` 与 `locked_fields`——等于永久挡住该字段以后被刮削填充。
     计划正文本来就是这么写的（Task 7 的 `applyUserEdit` 原样如此），要不要改属于设计决定，
     留给下一阶段定夺；最省事的做法是在控制器上按 `MetadataFields.STANDARD` 校验键名后 400。
  2. `VideoMetadataStore` / `ImageMetadataStore` 的 `rawResponseAsJsonb` 对空字符串会原样返回
     `""`，`CAST('' AS jsonb)` 会报错——Jackson 3 的 `readTree("")` 返回 `MissingNode` 而不是
     `null`（jshell 实证），所以那句 `parsed != null` 拦不住。当前所有提供者路径都不会产出空的
     rawResponse（空响应在更早一步就变成 `Optional.empty()`），属于潜在边界而非现存缺陷；
     真要补就是加一个 `!parsed.isMissingNode()`。
  3. §9 记的其余 Minor 保持原状：毫秒级 ETag 可能碰撞、补齐始终从 id 最小的 500 个开始可能
     饥饿、`continueReading` / `continueWatching` 不按库访问权过滤、`VideoProgressService.record`
     缺库访问权校验、`README.md` 模块图仍停在计划 01。
- **合并**：`git merge --no-ff` 合入 `main`，合并提交 `2e8c750`，tag `plan-05-preview_metadata`，
  已推送 `origin/main` 与该 tag。合并前先把 `D:\MyMedia` 里一处未提交的文档改动
  （删掉 §9 已过时的 opencode 环境说明）单独提交为 `dad5772`，三方合并正确保留了该删除。
- **收尾**：计划 05 的 worktree `D:\MyMedia-5` 与分支 `plan05-2026-08-17-05-preview-metadata`
  已删除（内容全部在 `main` 上，tag `plan-05-preview_metadata` 仍在）。
  **删之前先把 SDD 工作区归档了**：`.superpowers/` 是 git-ignored 的，`git worktree remove`
  会连它一起删干净——计划 03、04 的 ledger 与 brief/report 当年就是这么没的。计划 05 的那份
  已复制到 `D:\MyMedia\.superpowers\sdd\2026-08-17-05-preview-metadata`（36 个文件，
  progress、12 份 task brief/report、各次 review 的 diff），与 01、02 放在一起。
  **以后删任何 worktree 之前，先做这一步。**
- **下一步**：计划 06 已开始；当前工作区、Task 1 产物与下一任务入口见 §11。

## 11. 计划 06 执行交接（2026-08-25，Task 12 完成）

- **工作区与状态**：`D:\MyMedia-6`，分支 `plan06-2026-08-17-06-search-and-upload`；Task 1–12 已完成，尚未合入 `main`。后续任务继续在这个 worktree 上执行，不要在 `main` 直接改动。
- **Task 1 提交**：`1b39e43 feat: 添加搜索生成列、索引与查询规范化`。
- **Task 1 产物**：新增 `V12__search_columns.sql`，给 `video_item` / `image_node` 添加双参数 `to_tsvector('english', ...)` 生成列；新增视频 FTS、视频原名 trigram、图片 FTS、图片标题 trigram 四个索引。新增 `shared.SearchQuery`，负责空白规范化、Locale.ROOT 小写、LIKE 元字符转义和按码点判断三元组索引是否有帮助；新增 9 个纯单元测试与 5 个 PostgreSQL schema 集成测试。
- **Task 1 验证与审查**：RED 阶段在缺少 `SearchQuery` 时按预期于测试编译阶段失败；focused `SearchQueryTest,SearchSchemaTest` 为 14/14；全量验证为 485 tests、0 failures、0 errors、2 skipped、`BUILD SUCCESS`。Task 1 独立 review 为 spec compliant，Critical/Important/Minor 均为 0。
- **Task 2 提交**：`3cca797 feat: 添加视频域双路径搜索`；task review 发现分层排序缺陷后，`68dffd0 fix: 修正视频搜索分层排序` 修正并通过 scoped re-review。
- **Task 2 产物**：新增 `VideoSearchHit`、`VideoSearchService`、`VideoSearchController` 与 `VideoSearchServiceTest`。服务使用 `NamedParameterJdbcTemplate` 合并标题/原名 ILIKE 子串路径和 `search_vector` 英文 FTS 路径，按视频库访问权过滤，空可访问库提前返回；排序为子串命中优先、子串按 trigram 相似度、FTS-only 按 `ts_rank`、最后按 `sort_title`，控制器提供 `GET /api/video/search?q=&limit=` 并将 limit 限制在 1–100。未修改 V1–V12、图片域或后续任务文件。
- **Task 2 TDD 与验证**：先只加入测试并在 `VideoSearchService` 缺失时于测试编译阶段失败；排序回归测试在旧实现下失败，修复后通过。fresh focused `VideoSearchServiceTest,ModularityTests` 为 15 tests、0 failures、0 errors、0 skipped；全量 `mvn -B -ntp verify` 为 **498 tests、0 failures、0 errors、2 skipped，`BUILD SUCCESS`**。测试仍有既有 Mockito 动态 agent / Spring 测试配置警告；2 个 skipped 为既有环境限制。
- **Task 2 审查与遗留**：初次 task review 的 1 个 Important 已修复，scoped re-review 结论为 PASS，无新的 Critical/Important。遗留 deferred Minor：Task 2 未单独增加 `_`/反斜杠场景与 HTTP 控制器测试；LIKE 转义行为已有 Task 1 `SearchQueryTest` 覆盖，控制器由 Spring 上下文与 `ModularityTests` 验证可装配。
- **SDD 记录**：`.superpowers/sdd/2026-08-17-06-search-and-upload/progress.md`，Task 2 brief、report 及两轮 review package 均在该目录中。

**Task 3 完成（2026-08-24）**

- **提交**：`2f68466 feat: 添加图片域双路径搜索`。
- **产物**：新增 `ImageSearchHit`、`ImageSearchService`、`ImageSearchController` 与 `ImageSearchServiceTest`。与视频域两处刻意差异：搜 `name`（原名，一定有）与 `title`（刮削标题，可能为空）两个字段；只搜 `status = 'ACTIVE'` 节点（图片域文件消失只标 `MISSING` 不删，搜出点开就 404 的节点体验差）。`GET /api/image/search?q=&limit=`，limit 限制在 1–100，行为与视频域端点对称。未创建迁移（V13 仍留给 Task 5），未修改视频域、图片域既有文件之外的任何代码。
- **已知缺陷提前预防**：Task 3 的 brief 文本是在 Task 2 review 发现两个真实缺陷之前写的，草稿 SQL 与 Task 2 最初提交同形——(1) `title` 为 `NULL` 时 `(name ILIKE ... OR title ILIKE ...)` 结果是 SQL `NULL` 不是 `false`，`ORDER BY ... DESC` 排序不稳定；(2) 扁平 `ORDER BY substring_hit DESC, trgm_score DESC, fts_score DESC` 让子串命中层被 `fts_score` 干扰、FTS-only 层被 `trgm_score` 干扰。派发实现任务时直接指示搬运 `video/VideoSearchService.java` 已经过 review 验证的修复（`coalesce(..., false)` + `WITH scored AS (...)` CTE + 分层 `CASE WHEN` 排序），而不是照抄 brief 草稿、走一遍"实现→review 抓同一个缺陷→修复"的循环。Task review 逐行核对了这处搬运，确认正确适配（`name`/`title` 双字段、`status = 'ACTIVE'` 过滤、`sort_key` 兜底），Approved，无 Critical/Important。
- **测试与审查**：`ImageSearchServiceTest` 8/8、`VideoSearchServiceTest` 13/13（无回归）、`ModularityTests` 2/2；全量 `mvn -B -ntp verify` 为 **506 tests，0 failures，0 errors，2 skipped，BUILD SUCCESS**。Task review 一次通过，无 Critical/Important。遗留 deferred Minor：`ImageSearchService` 未加视频域那句"查询过短、trgm 索引不起作用"的 debug 日志（ADR-006 观测一致性，非 brief 硬性要求）；无独立 `ImageSearchController` HTTP 单元测试（与 Task 2 的 `VideoSearchController` 同一个已接受的缺口）。
- **执行中一次环境插曲**：实现子代理在跑集成测试前 Docker Desktop 未就绪，等待自建的后台监控但没有真正收到通知、两次静默结束回合；由控制器直接跑 `docker info` 确认守护进程已就绪后，指示子代理停止等待监控、直接同步跑 Maven 命令，随后顺利完成。不是代码问题，记录下来避免下次误判为任务卡住。
- **SDD 记录**：`.superpowers/sdd/2026-08-17-06-search-and-upload/progress.md`，Task 3 brief、report 及 review package 均在该目录中。

**Task 4 完成（2026-08-24）**

- **提交**：`3265cc1 feat: 创建 web 模块与全局搜索端点`。
- **产物**：新增 `web` 模块（`allowedDependencies = {"shared", "user", "video", "image"}`，spec §4.2 早已声明、前五份计划都没用到）；`GlobalSearchDto.Response(query, video, image)` 与 `GlobalSearchController`，`GET /api/search?q=&limit=` 顺序调用 `VideoSearchService.search`/`ImageSearchService.search`（未并行化，两条走索引的 SQL 加起来几十毫秒内，引入线程池是用复杂度换不需要的性能），结果**分区返回不混排**——未命中的一边是空数组而非省略字段。`GlobalSearchControllerTest` 5 个集成测试覆盖分区不混排、空命中域为空数组、空白 query 400、未登录 401、无库访问权用户两个分区皆空。未修改 V1–V12、视频域、图片域或 `SearchQuery`。
- **与 brief 的偏差**：仅测试文件一处环境事实修正——`@AutoConfigureMockMvc` 的导入从 brief 草稿的 Boot 3 路径（`org.springframework.boot.test.autoconfigure.web.servlet`）改为本仓库锁定的 Boot 4.1 路径（`org.springframework.boot.webmvc.test.autoconfigure`），与既有 `VideoStreamControllerTest`/`VideoBrowseControllerTest` 一致；照抄 brief 原路径会在测试编译阶段直接失败。生产代码三个文件与 brief 逐字一致。
- **TDD 与验证**：RED 阶段 `/api/search` 不存在，4/5 测试因 404 失败（`requiresAuthentication` 因 Security 早于路由匹配而已经通过）；加入生产代码后 focused `GlobalSearchControllerTest,ModularityTests` 7/7；全量 `mvn -B -ntp verify` 为 **511 tests，0 failures，0 errors，2 skipped，BUILD SUCCESS**（Task 3 基线 506 + 本任务新增 5，无回归）。
- **审查**：`/code-review medium` 一次通过，无 Critical/Important。1 处 deferred Minor：`MAX_LIMIT = 100` + `Math.clamp(limit, 1, MAX_LIMIT)` 在 `GlobalSearchController`、`VideoSearchController`（Task 2）、`ImageSearchController`（Task 3）三个控制器里逐字重复，未抽到 `shared`——与本项目"不为几行重复代码搭抽象"的既有取舍一致，本次不修。
- **SDD 记录**：`.superpowers/sdd/2026-08-17-06-search-and-upload/progress.md`，Task 4 report 见同目录 `task-4-report.md`。

**Task 5 完成（2026-08-24）**

- **提交**：`90610cf feat: 添加标签模型与管理端点`。
- **产物**：标签归 `metadata` 模块（不新增模块间依赖，`package-info.java` 未改动）。新增 `V13__tags.sql`：`tag(id, domain, name, slug, created_at)` + `video_item_tag` / `image_node_tag` 两张关联表，域分区复用 ADR-001 的复合外键手法第三次——`tag(id, domain)` 唯一键 + 关联表冗余 `domain` 列（`CHECK` 钉死取值）+ 复合外键指回 `tag`，让跨域打标签在数据库层面就不可能。`TagSlug`（package-private，小写化 + 空白/标点折叠成连字符，中文保留原字不做音译，全标点名字拒绝）、`TagRepository`、`TagService`（`findOrCreate`/`findByDomain`/`getById`/`delete`）、`TagDto`/`TagController`（`GET /api/tags?domain=` 全登录用户可读，`POST`/`DELETE` 限 ADMIN）。新增 `TagSlugTest`（8 例）与 `TagServiceTest`（7 例，含直接 JDBC 插入验证数据库拒绝跨域标签）。
- **实现方式**：brief 含 9 个文件的完整逐字内容（迁移、实体、仓储、slug 工具、服务、DTO、控制器、两个测试类），属转录型任务，派发给 haiku 档实现子代理；实现代理报告零偏差，全部文件与 brief 逐字一致。
- **TDD 与验证**：RED 阶段测试编译失败（`TagService` 符号不存在）；GREEN 后 focused `TagSlugTest,TagServiceTest,ModularityTests` 17/17；全量 `mvn -B -ntp verify` 为 **526 tests，0 failures，0 errors，2 skipped，BUILD SUCCESS**（Task 4 基线 511 + 本任务新增 15，无回归）。
- **审查**：spec ✅ 逐文件核对与 brief 字节级一致；审查员额外验证了 diff 之外的接线——`SecurityConfig` 默认 `.anyRequest().authenticated()` 覆盖了未加 `@PreAuthorize` 的 `GET /api/tags`、`@EnableMethodSecurity` 支撑 create/delete 上的 `@PreAuthorize`、`GlobalExceptionHandler` 正确把 `TagSlug.of` 的 `IllegalArgumentException` 转 400、`NotFoundException` 转 404；并确认 `V13` 是 `V12` 之后正确的下一个迁移号、`metadata/package-info.java` 的 `allowedDependencies` 未被改动。无 Critical/Important，Task quality: Approved。1 处 deferred Minor：`TagService.findOrCreate`（brief 原文如此）存在先查后建的竞态——两个并发请求都未命中查询、都尝试插入时，输家会从 `uq_tag_domain_slug` 唯一约束拿到 `DataIntegrityViolationException`，经 `GlobalExceptionHandler` 转译为 409（不会泄漏 500），属低并发 ADMIN-only 路径上的可接受权衡，未修。
- **SDD 记录**：`.superpowers/sdd/2026-08-17-06-search-and-upload/progress.md`，Task 5 brief、report 及 review package 均在该目录中。

**Task 6 完成（2026-08-24）**

- **提交**：`5669e93 feat: 添加打标签与按标签浏览`。
- **产物**：新增 `TagLinkStore`（package-private，读写 `video_item_tag`/`image_node_tag`）；`TagService` 新增 `tagsOf`/`setTags`/`targetIdsWithTag` 三个方法（`setTags` 是整体替换，前端多选框语义，天然幂等）；`TagLinkController` 提供 `GET|PUT /api/video/items/{id}/tags`、`GET|PUT /api/image/nodes/{id}/tags`、`GET /api/tags/{id}/items`；`VideoItem` 补上只读 `coverAssetId` 映射（`ImageNode` 在计划 04/05 已有，本任务不必再改）；`VideoCatalogService`/`ImageCatalogService` 各新增保持入参顺序的 `findByIds`。按标签列条目绕批量查询领域 API 走，不直接 SELECT 领域表——与计划 05 「`preview` 不直接 SELECT `video_item`」同一条纪律。跨域打标签在服务层先判一次给出可读的 `IllegalArgumentException`（→ 400），数据库复合外键仍是最后一道防线。
- **落实计划里预先埋的三处伏笔之一**：`derived_asset.source_scanned_file_id` 在 V10 里落地成 `NOT NULL`（计划正文的最后一条测试用例原按可空写，且明确写了「若改成 NOT NULL 就先插一行 `scanned_file`」）——GREEN 阶段跑出真实的 NOT NULL 违例后按此修正，不是 review 才发现。§5 那张「计划 06 执行时依赖上游落地形态的三处」现在可以划掉这一条。
- **偏差**：仅测试文件的 `@AutoConfigureMockMvc` 导入按 Task 4 已确认的 Boot 4.1 路径修正（`org.springframework.boot.webmvc.test.autoconfigure`），生产代码 8 个文件与计划正文逐字一致。
- **TDD 与验证**：RED 阶段 `TagService.setTags` 符号不存在；GREEN 后 focused `TagLinkTest,TagServiceTest,ModularityTests` 20/20（11+7+2，无回归）；全量 `mvn -B -ntp verify` 为 **537 tests，0 failures，0 errors，2 skipped，BUILD SUCCESS**（Task 5 基线 526 + 本任务新增 11）。
- **审查**：`/code-review medium` 一次通过，无 Critical/Important。唯一的 Minor 是 Task 5 就已发现并 deferred 的 `TagService.findOrCreate` 先查后建竞态——核对过方法体与 Task 5 逐字节相同（本任务只加了构造参数与三个新方法），沿用原裁决不改。
- **SDD 记录**：`.superpowers/sdd/2026-08-17-06-search-and-upload/progress.md`，Task 6 report 见同目录 `task-6-report.md`。

**Task 7 完成（2026-08-25）**

- **提交**：`3abf4e7 feat: 添加两个域的收藏` → 修复 `4b5b8a7 fix: 收藏列表按 limit 下推到数据库查询`。
- **产物**：新增 `V14__favorites.sql`（`video_favorite` / `image_favorite` 两张复合主键表，`user_id` + 目标 id，`ON DELETE CASCADE`，各带一个 `(user_id, created_at DESC)` 索引）。两个域各自的 `VideoFavorite`/`ImageFavorite`（`@IdClass` 复合主键实体）、`*FavoriteRepository`、`*FavoriteService`（`add`/`remove`/`isFavorite`/`listItems`|`listNodes`，增删幂等）、`*FavoriteController`（`PUT|DELETE /api/{video,image}/{items,nodes}/{id}/favorite`、`GET /api/{video,image}/favorites`）。收藏功能**刻意不抽到公共模块**——与播放/阅读进度同一条纪律：抽公共模块会立刻需要一个「目标类型」多态列，这是本项目一直在避免的东西。图片域允许收藏纯目录（`ImageNode.isReadable()` 为 `false` 的节点也能收藏，spec §6.5 明写）。控制器一律用 `LibraryAccessService.canAccess` 校验后抛 `NotFoundException`（404 不泄露存在性）。
- **实现方式**：brief 含 11 个文件的完整逐字内容，属转录型任务，派发给 haiku 档实现子代理。
- **环境插曲（不是代码问题，但值得记录）**：实现子代理跑集成测试时遇到 `Could not find a valid Docker environment`（Testcontainers 连不上 `npipe:////./pipe/dockerDesktopLinuxEngine`），但**错误地**把这个判定为「环境问题、预先存在、不影响交付」并自报 `DONE`——且从未真正看到一次测试通过。它的报告里还有一条经核对为**假**的断言：「上一个提交基线同样是 537 tests、306 errors」，与 Task 6 交接记录的干净 537/0/0/2 矛盾。控制器用 `docker info`/`docker ps` 核实后发现 Docker Desktop 的引擎进程根本没启动（只有 CLI 客户端能响应），启动 Docker Desktop、等待守护进程可连接后，自己重跑了全部测试——**这是一条通用经验**：碰到"Testcontainers 连不上 Docker"，先确认 `docker info` 的 `Server:` 段是否存在，CLI 能响应不代表引擎在跑（计划 06 Task 3 也踩过一次同类问题，但那次是 Docker Desktop 还没来得及启动完成；这次是它压根没被启动）。
- **控制器独立验证**（不采信实现子代理自己的测试主张，因为它刚证明过不可靠）：focused `mvn -B -ntp test -Dtest='VideoFavoriteServiceTest,ImageFavoriteServiceTest,ModularityTests'` 19/19（9 图片 + 8 视频 + 2 模块化）；全量 `mvn -B -ntp verify` **554 tests，0 failures，0 errors，2 skipped，BUILD SUCCESS**（Task 6 基线 537 + 本任务新增 17，无回归）。
- **审查与修复**：`/code-review`（sonnet 档）：spec ✅ 逐文件核对 11 个文件与 brief 一致，404-not-403、`ModularityTests` 域解耦、UUID 测试隔离均已确认。Task quality: Approved，但带 1 条 Important（标记 plan-mandated——brief 原文如此）：`listItems`/`listNodes` 用 `repository.findByUserIdOrderByCreatedAtDesc(userId)` 取回用户**全部**收藏再在 Java 里 `.limit()`，没有把 `limit` 下推到数据库，与同一代码库里 `VideoProgressService.continueWatching`/`ImageProgressService.continueReading` 已经在用的 `Pageable` 写法不一致。裁决为**该改**（不是"plan 这么写就照单全收"）：这不是设计取舍，是能直接照抄本仓库既有姊妹方法的机械修正。修复：两个 Repository 加 `@Query` + `Pageable` 重载，两个 Service 改用 `PageRequest.of(0, limit)`，公开方法签名不变。fix round 1/5：haiku 档 scoped re-review 判定 ADDRESSED，无新增 Critical/Important。修复后全量验证仍为 554/0/0/2。
- **裁决**（记入 SDD ledger 的 Ruling）：Important 发现虽然是 brief 逐字代码，但本仓库已有现成的姊妹写法（`continueWatching`/`continueReading`），修正是机械性的、低风险的，判定为**该修不是该忍**——不同于"plan 明确要求某种设计"的那类 plan-mandated 争议。
- **搁置的 Minor**（不阻塞，记账）：1）`add()` 先查后建的竞态（brief 原文如此，复合主键仍保证不出现重复行，低并发风险）；2）`DELETE .../favorite` 端点缺一条无权访问返回 404 的测试（`PUT` 已测，两者共用同一段 `requireAccess`，覆盖面缺口而非行为缺陷）。
- **SDD 记录**：`.superpowers/sdd/2026-08-17-06-search-and-upload/progress.md`，Task 7 brief/report/两轮 review 的 diff 包均在该目录。

**Task 8 完成（2026-08-25）**

- **提交**：`4137ace feat: 添加分享链接的创建、撤销与查询`。
- **产物**：新增 `V15__share_link.sql`（`share_link` 表：`token`、`library_id`、可空的 `video_item_id`/`image_node_id`、可空 `password_hash`/`expires_at`、`created_by`、`revoked_at`，`CONSTRAINT ck_share_link_single_target CHECK (num_nonnulls(video_item_id, image_node_id) = 1)` 替代多态列，两个目标外键都是 `ON DELETE CASCADE`）。`ShareLinkService` 住 `library`（依赖表不变，仍是 `{"shared","user"}`），只存标量 id，从不 import `video`/`image` 类型；`ShareLink`/`ShareGrant`/`ShareLinkDto` 同住 `library`。令牌是 32 字节 `SecureRandom` 的 Base64URL 无填充编码（43 字符，不用 `UUID`）。密码走既有 `PasswordEncoder`（`{bcrypt}` 前缀），响应体只给 `passwordProtected` 不给哈希。`resolve()` 对不存在/已过期/已撤销一律抛同一个 `NotFoundException`。创建端点被迫拆成两半：`POST /api/video/items/{id}/share`（`video.web`）与 `POST /api/image/nodes/{id}/share`（`image.web`）——因为创建前要校验目标访问权，需要 `VideoCatalogService`/`ImageCatalogService`，而 `library` 不许依赖这两个域；`GET /api/shares`、`DELETE /api/shares/{id}` 只认 `created_by`，留在 `library`。管理端点响应只含标量字段（不含标题/封面），前端按 `domain` 各自去取。未引入 SPI 倒置（ADR-004 的适用条件不满足）。
- **执行中的插曲**：第一个实现子代理（haiku 档，brief 含 11 个文件完整逐字内容，转录型任务）在写完全部 11 个文件、尚未跑测试或提交前被用户中断终止，未留下报告。控制器接手后先把磁盘上已有的 11 个文件逐行核对 brief（含预先标记的 Boot 4.1 `@AutoConfigureMockMvc` 导入修正），确认全部正确，再自己跑测试——`docker info` 确认引擎已在运行（这次没重演 Task 7 那次 Desktop 没启动的插曲）。跑出一个真实缺陷：`ShareLinkServiceTest.anExpiredTokenIsNotFound` 直接把 `java.time.Instant` 传给 `JdbcTemplate.update(...)`，抛 `PSQLException: Can't infer the SQL type to use for an instance of java.time.Instant`——本项目的 pgjdbc 驱动对裸 `Instant` 没有自动类型推断（JPA 实体上的 `Instant` 字段没事，因为走 Hibernate 的类型转换；这是全项目第一次有代码通过裸 `JdbcTemplate` 写 `Instant`）。裁决为 brief 本身的缺陷、诊断已经做完，派发第二个实现子代理（haiku 档，单行机械修复）直接给出确诊与修复方案：把该行改成 `java.sql.Timestamp.from(Instant.now().minus(...))` 再传给 `jdbc.update`，不碰生产代码。
- **测试与审查**：第二个实现子代理验证 focused `ShareLinkServiceTest,ShareLinkControllerTest,ModularityTests` 21/21（12+7+2）；全量 `mvn -B -ntp verify` 为 **573 tests，0 failures，0 errors，2 skipped，BUILD SUCCESS**（Task 7 基线 554 + 本任务新增 19，无回归）。Task review（sonnet 档）：迁移纪律（只碰 V15）、模块依赖方向（`library` 依赖表未变、`ShareLinkService`/`ShareLink`/`ShareGrant` 不 import `video`/`image`、`video`/`image` 已声明可依赖 `library`）、404-not-403（两个创建端点与 `revoke`）、令牌设计、`resolve()` 对失效令牌的统一 404、密码处理、外键与 CHECK 设计、`GET /api/shares` 只给标量、未引入 SPI 倒置、Boot 4.1 导入修正逐项对着 diff 核实（部分还核对了 diff 之外的既有代码），无 Critical/Important，Task quality: Approved。2 处 deferred Minor：新文件的多行参数续行缩进不一致（纯格式，本项目没有格式化门禁会拦下它）；三个控制器都重复了一段 `userQueryService.findByUsername(...).orElseThrow(...)` 查 id 的代码（审查员确认这是沿用早前任务已有的写法，不是本任务引入的新重复）。
- **SDD 记录**：`.superpowers/sdd/2026-08-17-06-search-and-upload/progress.md`，Task 8 brief/report 均在该目录。

**Task 9 完成（2026-08-25）**

- **提交**：`bc544fd feat: 分享链接的免登录访问` → 修复 `a6bea66 fix: 分享端点流式响应的异步测试竞态`。
- **产物**：`/api/share/**` 整段 `permitAll`（`SecurityConfig`），令牌本身即凭证——路径模式按整段比较，不会误放行需要登录的 `/api/shares`（复数），有专门测试钉住。新增 `ShareTicket`（`library` 包私有）：带密码的链接解锁后签发 `<过期 epoch 秒>.<base64url HMAC-SHA256>` 短期票据，服务端不存状态，校验用 `MessageDigest.isEqual` 定时安全比较，过期时刻取 `min(配置的 TTL, 链接自身过期时刻)`——避免每次翻页都重验一次约 100ms 的 bcrypt。`ShareLinkService` 新增 `unlock`（校验密码签票据）与 `resolveUnlocked`（解析令牌 + 校验票据，令牌失效一律 404，票据缺失/错误 401）。新增 `ShareAccessController`（`GET /api/share/{token}` 免票据可读、`POST /api/share/{token}/unlock`）。`VideoStreamService`/`ImagePageService` 新增 `locateForShare`：不查 `library_access`（令牌已经代表授权），但查包含性（分享条目 A 的链接不能读条目 B 的文件；图片域按物化路径前缀匹配整棵子树）——包含性校验钉死在服务层而不是控制器层。`VideoStreamController` 的 Range/206/ETag/If-Range/416 应答逻辑抽成 `VideoRangeResponder`（静态工具类），登录端点与 `VideoShareController`/`ImageShareController` 两个分享端点共用同一段代码，未复制播放/阅读逻辑。
- **入口状态与本任务的特殊之处**：与 Task 1–8 不同，本任务开始时工作区里已经有一份未提交的草稿——上一个会话把 brief 描述的全部 14 个文件都写好了，但没跑测试、没自评、没提交（据用户说明：那次会话「未能正确加载 skills」，即绕开了本计划一直在用的 subagent-driven-development 流程直接写代码）。处理方式与 Task 8 对待"被中断的第一个实现子代理留下的孤儿文件"一致：当作未经验证的草稿，不当作已完成的工作。
- **派发前预诊断的一个真实缺陷**：控制器在派发实现子代理之前先对着草稿跑了一次 focused 测试，发现 `VideoShareControllerTest.java` 插入 `video_file` 用了 `role = 'MAIN'`，而 `V6__video_domain.sql` 的 `ck_video_file_role` 只允许 `PRIMARY/VERSION/EXTRA/SUBTITLE/TRAILER`——`'MAIN'` 不是合法值，`setUp()` 每次都因 `DataIntegrityViolationException` 失败。全代码库其余插入 `video_file` 的测试（如 `VideoDomainConstraintTest`）用的都是 `'PRIMARY'`。诊断连同修复方案一并交给实现子代理（haiku 档），不用它自己重新排查。
- **实现子代理自己的测试判断不可信、被控制器纠正**：实现子代理修好 `'MAIN'`/`'PRIMARY'` 后报告 focused 22/24、全量 595/597，但把 2 个失败的流式测试（`anAnonymousVisitorCanReadTheItemAndStreamIt`、`rangeRequestsWorkThroughTheShareEndpointToo`）诊断为「Windows 并行执行 / 临时文件资源争用，不阻塞」并自报 `DONE`。控制器独立跑 `mvn -B -ntp clean verify` 复现了同样的 2 个失败（确定性，非偶发），并核对 `pom.xml`——**全项目没有任何 Surefire `<parallel>`/`forkCount` 配置**，Surefire 默认在单个 fork 里顺序跑测试类，"并行执行"这个解释本身就不成立。真实原因：`VideoShareController.stream()` 返回 `ResponseEntity<StreamingResponseBody>`，由 Servlet 后台线程异步写出；brief 草稿给这两个流式断言写的是裸 `mockMvc.perform(get(...)).andExpect(content().bytes(...))`，没有先等异步生命周期完成——这正是总览文档本节（§2）早就记录在案的坑："`StreamingResponseBody` 的 MockMvc 测试先等待异步生命周期完成，再执行 `asyncDispatch`"。姊妹测试 `VideoStreamControllerTest.streamRequest()`（登录端点）已经正确实现了这个模式且 10/10 通过。是一个确定性的竞态（负载越高越容易输，所以"单独跑过、全量跑不过"不是环境问题的证据，反而是竞态的典型signature），与 Task 8 的 `Instant`/`JdbcTemplate` 缺陷同一类型——brief 草稿测试代码里第一次出现、只在真正跑测试时才会暴露的缺陷。
- **裁决**（记入 SDD ledger 的 Ruling）：诊断已经做完（根因、确切修复、可直接照抄的参考实现都已确定），恢复同一个实现子代理，给出诊断与修复方案（照抄 `VideoStreamControllerTest.streamRequest()` 的 `asyncStarted()`/`getAsyncResult()`/`asyncDispatch()` 模式，只改测试文件的这两个断言，不碰生产代码），而不是接受"不阻塞"的说法或另起一轮完整诊断。——为什么：根因和修复已经从复现的失败与仓库里现成的参考实现里确凿地推出，重新排查是浪费；接受"不阻塞"会把一个确定性竞态的 flaky 测试带进套件。若判断有误的代价：未观察到——修复编译通过、经 diff 核对与参考实现逐句一致，且连续两次从头跑 `clean verify` 都是绿的。
- **测试与审查**：修复提交 `a6bea66` 经 diff 确认只碰了测试文件（`VideoShareControllerTest.java`，+22/-3 行），不碰生产代码。控制器独立验证（连续两次、每次都从 `clean` 开始）：**597 tests，0 failures，0 errors，2 skipped，BUILD SUCCESS**（Task 8 基线 573 + 本任务新增 24——`ShareTicketTest` 8、`VideoShareControllerTest` 10、`ImageShareControllerTest` 6，无回归）。Task review（sonnet 档）：14 个 brief 列出的文件逐一核对有对应 diff；`ShareTicket` 的定时安全比较（`MessageDigest.isEqual`，非 `String.equals`）与畸形票据不抛异常均已核实；TTL 钳制 `min(配置 TTL, 链接过期时刻)` 对着 `theTicketNeverOutlivesTheLinkItself` 核实；`/api/share/**` 放行但 `/api/shares` 仍需登录有专门测试钉住；两个域的包含性校验（视频比对 `itemId`，图片按物化路径前缀，两个方向都测了）已核实；`VideoStreamController`→`VideoRangeResponder` 的抽取经逐段 diff 比对确认与被删除的内联逻辑字节级等价（保留的是既有更健壮的直接缓冲区兜底实现，比 brief 草稿的简化版更安全，判定为合理决策而非回归）；异步 MockMvc 修复模式经对照未改动的参考文件确认逐句一致；模块依赖方向确认（`library` 新增的分享相关类未 import `video`/`image`）。无 Critical/Important，Task quality: Approved。3 处 deferred Minor：`VideoShareController`/`VideoShareControllerTest` 各有一处内联全限定名而非 import（纯风格）；`TICKET_HEADER = "X-Share-Ticket"` 在 `VideoShareController`/`ImageShareController` 里各自重复定义而未提到双方都已依赖的 `library`（单字符串字面量的轻微重复）；`ImageShareControllerTest` 有「图片令牌不能用于视频端点」的测试，`VideoShareControllerTest` 没有对称的「视频令牌不能用于图片端点」测试（两个方向的守卫代码结构相同，风险低，但覆盖不对称）。
- **SDD 记录**：`.superpowers/sdd/2026-08-17-06-search-and-upload/progress.md`，Task 9 brief/report 均在该目录。

**Task 10 完成（2026-08-25）**

- **提交**：`7772a03 feat: SampledHash 提升到 shared，新增上传会话与秒传`（单提交，Steps 1–13 一次性完成）。
- **产物**：`SampledHash` 从 `com.mymedia.scan`（package-private）搬到 `com.mymedia.shared` 并改 `public`——算法一个字节没动（diff 里 `SampledHashTest.java` 只有包声明那一行变化），与 `NaturalSortKey`、`MaterializedPath` 并列，践行「复用算法，不复用模型」。`scan` 新增 `ScannedFileHashService`（`findActiveByContentHash` / `findActiveBySizeWithoutHash` / `computeAndStoreHash`），刻意不并进 `ScannedFileQueryService`——那个类的 javadoc 明写「不能修改物理层状态」，写方法塞进去只会让那句话变成谎话。新增 `upload` 模块（`allowedDependencies = {"shared","user","library","jobs","scan"}`，**不依赖 `video`/`image`**）：`InstantUploadResolver` 实现两步秒传（先按 `content_hash` 直查，未命中再取同库同尺寸且哈希为空的候选、上限 8 个、现算并写回再比，候选文件在磁盘上消失时跳过而不抛异常）；`SafeFileName` 净化上传文件名（两种路径分隔符、纯点名、控制字符、Windows 保留字符 `<>:"|?*`、结尾点空格、超长截断保留扩展名、净化后为空全部各有单测）；`UploadSessionService.create` 服务端决定分片大小与片数（`UploadDto.CreateRequest` 里没有分片大小字段，客户端不能覆盖）、拒绝无权限库（404 而非 403）、拒绝超限文件、秒传命中直接完成；`GET/POST /api/upload/sessions[/{id}]` 两个端点，`contentHash` 在 DTO 层用 `@Pattern(regexp = "[0-9a-f]{64}")` 卡掉格式错误。V16 迁移建 `upload_session` + `upload_chunk` 两表（分片到达高频幂等、会话低频状态机，故意拆两张表）。分片收片与合并留给 Task 11——本任务的 `UploadController` 只有会话创建与查询两个端点。
- **执行中的插曲**：实现子代理（haiku 档，brief 含全部文件完整逐字内容，转录型任务）第一次派发在 Step 11（创建 `InstantUploadResolver`）附近被一次基础设施 API 错误（"No response from API"）中断，没留下报告也没提交。控制器直接检查工作区（`git status` + 文件清单），确认 Step 1–11 已经落盘且结构完整（`SampledHash` 已搬家并 staged 为 rename、`ScannedFileHashService`、`RelocationDetector`/`ScannedFileRepository` 的改动、`upload/` 下除 `web/` 外的全部文件、三个测试文件全部就位），据此恢复同一个子代理（用它的 agentId 发消息），告知已完成的清单与剩余的 Step 12（`web/UploadDto.java`、`UploadController.java`、`application.yml` 两组配置）与 Step 13，并要求它先核对中断前写的文件有没有截断再继续。裁决：选择恢复而非重新派发——中断是纯基础设施问题（不是子代理卡住或做错方向），且磁盘状态已独立核实完整，重新派发只会让已经正确的 11 步白白重做一遍。若判断有误的代价：未观察到——恢复后的子代理只留下一个提交（没有返工痕迹），且随后的 task review 对全部文件逐行核对也没发现中断留下的截断或损坏。
- **测试与审查**：实现子代理最终验证 `SafeFileNameTest` 9/9、`InstantUploadResolverTest` 6/6、`UploadSessionServiceTest` 9/9、`SampledHashTest` 7/7（未变——计划文本估计的「8 个」是过时数字，这个文件在本任务之前就是 7 个测试，diff 只改了包声明一行）、`ModularityTests` 2/2；全量 `mvn -B -ntp verify` **621 tests，0 failures，0 errors，2 skipped，BUILD SUCCESS**（Task 9 基线 597 + 本任务新增 24：9+6+9，无回归；2 个 skipped 是既有的环境门控测试）。Task review（sonnet 档）：brief 列出的 19 个 diff 文件逐一核对处理方式（2 个 move、3 个 modify、12 个 create、3 个新测试）；发现 `ScannedFileRepository.java` 的改动没写进 brief 头部的「Files:」清单，但 Step 3 正文明确要求这个改动——判定为 brief 自身的文档缺口，不是实现子代理的偏离。12 条 Global Constraints 逐条对着 diff 核实，diff 上下文不够时还核对了现有源码：`SampledHash` 方法体逐字未变（只有包名/可见性/javadoc 变了）；`upload` 的 `allowedDependencies` 精确匹配、代码里没有任何 `video`/`image` import；`ScannedFileHashService` 是 `scan` 里的新类，`ScannedFileQueryService` 未被触碰；秒传两步策略逐行核实（含候选文件消失时跳过而非抛异常）；`SafeFileName` 的 9 条安全行为每条都有对应单测；`UploadDto.CreateRequest` 没有分片大小字段，`UploadSessionService.create` 只从 `UploadProperties` 算分片；`create()`/`get()` 的 404-not-403 均确认，且确认 `NotFoundException` 的全局 404 映射是既有代码；`contentHash` 格式校验确认；`UploadController` 确认只有会话创建/查询两个端点（分片收片按计划留给 Task 11）；未引入 JSONB/数组列的 JPA 映射；Boot 4.1 的 `AutoConfigureMockMvc` 导入修正确认是相对 brief 逐字草稿的唯一偏离（逐一核对了全部 9 个测试用例）。无 Critical/Important，Task quality: Approved。2 处 deferred Minor：`ScannedFileHashService.computeAndStoreHash` 在候选文件的 `scanned_file` 行在「查候选」与「算哈希」之间被删除时会抛 `NotFoundException`，是一个没测试覆盖的窄 TOCTOU 竞态，会在创建会话请求上表现为未处理的 404，而不是像磁盘文件消失那样被优雅跳过；`UploadSessionService` 构造器的参数续行缩进是 3 空格而不是本项目惯例的 4/8 空格，纯格式问题。
- **SDD 记录**：`.superpowers/sdd/2026-08-17-06-search-and-upload/progress.md`，Task 10 report 在该目录。

**Task 11 完成（2026-08-25）**

- **提交**：`b77b245 feat: 分片上传与断点续传`（单提交，Steps 1–7 一次性完成）。
- **产物**：`upload_chunk` 表沿用 Task 10 的 `V16__upload.sql`，本任务不新建迁移。新增 `UploadStorage`（package-private）：分片落在 `{tempRoot}/{sessionId}/{index}.part`，两级路径都是数字，用户可控的 `filename` 在这条路径上完全不出现（净化后的文件名只在 Task 12 合并阶段才用到）；`writeChunk` 最多读 `limit+1` 字节，多出来的那一个字节是探针，能读到就说明客户端超发了，不设上限的话一个撒谎的 `Content-Length` 就能把磁盘写满；先写 `.tmp` 再 `ATOMIC_MOVE` 改名，中途断线不留下"看起来完整"的半成品。新增 `UploadChunkStore`（package-private，`JdbcTemplate`）：`record` 是一条 `ON CONFLICT DO UPDATE` 的 upsert 让同一片重传天然幂等，`receivedIndexes`/`count` 两个只读查询。`UploadSessionService` 新增 `receiveChunk`（**刻意不是 `@Transactional`**——一个 8MB 分片在慢网络上可能写好几秒，若整个方法挂着事务会把连接池占满、拖垮应用其它部分；读会话是一次短事务，落盘不在事务里，记录分片又是一次短事务，事务边界贴着数据库操作不贴着业务动作）与 `receivedChunks`（断点续传的全部机制：客户端看一眼已收清单就知道要补哪几片，不需要服务端记住"传到第几个字节"）。`UploadController` 的 `GET` 端点从返回 `List.of()` 改为真正回传已收清单，新增 `PUT /api/upload/sessions/{id}/chunks/{index}`（`consumes = application/octet-stream`，直接从 `HttpServletRequest.getInputStream()` 流式落盘，不走 multipart——Boot 的 multipart 默认上限是 1MB/10MB 且会先落一遍临时文件）。`UploadChunkTest` 10 个用例覆盖乱序到达、幂等重传、清单升序、过短/过长分片各自 400、下标越界 400、别人的会话 404、非 `RECEIVING` 状态会话拒绝分片 409。本任务结束时全部分片到齐的会话仍停在 `RECEIVING`——触发合并是 Task 12 的事。
- **实现方式**：brief 含全部文件的完整逐字内容（`UploadStorage`/`UploadChunkStore`/`UploadChunkTest`）加两处现有文件的精确插入片段（`UploadSessionService`/`UploadController`），属转录型任务，派发给 haiku 档实现子代理；派发时额外把这两个文件改动前的构造函数签名与 `GET` 方法体贴进上下文，因为 brief 只给了要插入的片段、没给周边现状。实现代理无提问、无阻塞，一次报告 DONE。
- **测试与验证**：`UploadChunkTest` 10/10、`UploadSessionServiceTest` 9/9（无回归）、`ModularityTests` 2/2；全量 `mvn -B -ntp verify` **631 tests，0 failures，0 errors，2 skipped，BUILD SUCCESS**（Task 10 基线 621 + 本任务新增 10，无回归；2 个 skipped 是既有的环境门控测试）。
- **审查**：Task review（sonnet 档）：brief 列出的 5 个文件逐一核对有对应 diff 且签名相符；10 个 `UploadChunkTest` 用例逐条核实存在；未触碰任何 Flyway 迁移（对着 `V16__upload.sql` 现有 schema 逐列核对 `UploadChunkStore` 的 SQL）；404-not-403 按调用链核实（`receiveChunk` 先调用既有的 `get()`，复用它的 `NotFoundException` 路径，落在状态/下标检查之前）；Boot 4.1 的 MockMvc 导入已按惯例修正；探针字节 `limit+1` 读取上限按代码逐行核实；`receiveChunk` 非事务边界按代码核实（方法本身无 `@Transactional`，`get()` 与 `chunkStore.record()` 各自独立短事务，磁盘 I/O 夹在中间不在任何事务里）；分片路径纯净性核实（`UploadStorage` 里没有任何地方引用 `filename`）；`package-info.java` 确认未被触碰。无 Critical/Important，Task quality: Approved。3 处 deferred Minor：`receiveChunk`/`receivedChunks` 内部用隐式 `this` 调用 `get(...)`，绕开了 Spring AOP 代理，`get()` 自己的 `@Transactional(readOnly = true)` 对这类内部调用不生效——今天无害只是因为 `UploadSessionRepository.findById` 在 Spring Data 层本身就是独立事务的，javadoc 里"读会话是一次短事务"这句把保证归给了错误的层；并发场景下同一 `(sessionId, index)` 的重复 PUT 都写向同一个固定的 `.part.tmp` 路径且没有独占创建，重叠的客户端重试可能互相破坏分片内容而其中一个写手仍能通过大小校验，brief 的测试是顺序执行的没覆盖这个场景；`writeChunk` 的 try-with-resources 只在"大小不符"分支清理临时文件，`body.read()` 抛 `IOException`（比如客户端中途断连）时不会清理，异常会一路传到一个通用 500，留下的 `.tmp` 文件要等以后某次 `deleteSession` 才会被顺带清掉。
- **SDD 记录**：`.superpowers/sdd/2026-08-17-06-search-and-upload/progress.md`，Task 11 brief/report/review diff 均在该目录。

**Task 12 完成（2026-08-25）**

- **提交**：`1ea13cd feat: 分片合并、校验、入库与触发扫描` → 修复 `1c6455a fix: 上传端到端测试需要开启 JobPoller` → 修复 `133799d fix: 上传端到端测试改用 Awaitility 等待异步任务完成`。
- **产物**：本任务不新建迁移，`upload_session`/`upload_chunk` 沿用 Task 10 的 `V16__upload.sql`。`UploadStorage` 新增 `assembleInto(sessionId, totalChunks, target)`：用 `FileChannel.transferTo` 逐片零拷贝拼接，缺片直接抛 `IOException` 让调用方翻译成可重试的失败。`UploadSessionRepository` 从空壳接口新增 `markAssemblingIfReceiving`（`@Modifying(clearAutomatically = true)` 的条件 `UPDATE ... WHERE status = 'RECEIVING'`）——判断与写入压成一条语句，只有一个线程能拿到 `1`，与计划 05 `cover_asset_id` 回填是同一个手法在本项目第三次出现。`UploadSessionService.receiveChunk` 末尾追加：片齐且拿到条件 UPDATE 的 `1` 才入队 `UPLOAD_ASSEMBLE`（`dedup_key = upload-assemble:{id}` 是第二道防线，即便两个线程都拿到 `1` 也只会有一个任务）；新增 `markCompleted`/`markFailed`/`forAssembly` 三个 package-private 短事务方法。新增 `UploadAssembler`（**全类零 `@Transactional`**——合并 20GB 文件要读写 40GB，状态改动全部委托给 `UploadSessionService` 的短事务方法）：分片不齐或合并后大小/哈希不符，标 `FAILED` 并正常返回（任务算成功，因为"再合一百遍结果也一样"，重试没有意义）；读写异常直接抛出去，让任务表按指数退避重试。重名文件追加 `(2)`、`(3)`……绝不覆盖（扫描靠路径认文件，覆盖等于悄悄换掉别人的内容）。合并完成后 `scanTrigger.requestScan(...)`，语义层照常靠扫描链路建立。新增 `UploadAssembleJobHandler` 注册 `UPLOAD_ASSEMBLE` 任务类型。`UploadEndToEndTest` 5 个用例覆盖：断点续传到合并完成并触发扫描、秒传命中不产生第二份物理副本、重名冲突加后缀且不碰别人的文件、哈希不符标 FAILED 且任务算成功不会无限重试、合并任务在并发/重传下只入队一次。
- **实现方式**：brief 含 6 个文件的完整逐字内容（3 处已有文件的精确 diff、3 个新文件全文，含完整的端到端测试），属转录型任务，派发给 haiku 档实现子代理。
- **执行中的两处插曲，均由控制器独立验证发现、非实现子代理自报**（这是本计划第三次出现"实现子代理自称环境问题、控制器复现后发现是真缺陷"的模式，前两次是 Task 8 的 `Instant`/`JdbcTemplate`、Task 9 的异步 MockMvc 竞态）：
  1. 第一次提交后子代理报 `DONE_WITH_CONCERNS`（Docker Desktop 引擎未启动，编译过但没跑成测试）。控制器启动 Docker Desktop、等守护进程可连接后自己跑 focused 测试，发现全部 5 个 `UploadEndToEndTest` 用例都因 `No qualifying bean of type JobPoller` 失败——根因是 brief 草稿的 `@TestPropertySource` 写成了 `mymedia.jobs.enabled=false`，但测试要 `@Autowired JobPoller` 并手动 `pollOnce()`；`AbstractIntegrationTest` 默认就是 `false`，而 `JobScheduler`（`JobPoller` 唯一实现）是 `@ConditionalOnProperty(havingValue = "true")`，容器里根本没这个 bean。这正是总览文档本节（§2）记录过的坑，且已有 21 个测试文件在用正确写法（如 `VideoPreviewJobTest.java:36-37`）。控制器把确诊与修复方案（改 `true` 并加 `poll-interval=PT1H`）交给同一个子代理，机械修正，无需重新排查。
  2. 修复后 focused 测试仍是 `UploadEndToEndTest` 1/5——只有从不调用 `pollOnce()` 的 `theAssembleJobIsEnqueuedExactlyOnce` 通过。控制器追踪 `JobScheduler.pollOnce()`（`src/main/java/com/mymedia/jobs/JobScheduler.java:56-61`）确认它是**异步提交、同步返回**，不等任务跑完；brief 草稿的测试代码在单次 `pollOnce()` 后就直接断言终态，与异步执行赛跑。同样是总览文档 §2 记录在案的坑，且本仓库已有现成的 `await().atMost(...).untilAsserted(...)` 惯用法（`VideoPreviewJobTest.java:92-108`）。控制器把根因、参考实现与四个用例的精确替换代码一并交给同一个子代理机械搬运，第五个用例（不调用 `pollOnce()`）保持不变。
- **裁决**（记入 SDD ledger 的 Ruling）：两处都恢复同一个实现子代理而非重新派发或另起诊断——根因与修复都已经从复现的失败、`JobScheduler` 源码与仓库里现成的参考实现确凿推出，是机械搬运不是新设计。若判断有误的代价：未观察到，见下方控制器独立复核。另有一处子代理自发的偏离（`UploadAssembleJobHandler` 把 brief 里构造器注入的 `ObjectMapper objectMapper` 改成 `private static final ObjectMapper MAPPER = new ObjectMapper()`）——核对过与本仓库其余全部 `JobHandler`（`PreviewJobHandler`/`SpriteJobHandler`/`MetadataFetchJobHandler` 等）一致的既有惯例后判定为该留，不是范围蔓延，已明确告知子代理保留并原样转告审查员核实。
- **测试与审查**：控制器独立复核（Docker 守护进程已在运行，非自报）：focused `mvn -B -ntp test -Dtest='UploadEndToEndTest,UploadChunkTest,UploadSessionServiceTest,ModularityTests'` **26/26**（5+10+9+2）；全量 `mvn -B -ntp verify` **636 tests，0 failures，0 errors，2 skipped，BUILD SUCCESS**（Task 11 基线 631 + 本任务新增 5，无回归）。Task review（sonnet 档）：brief 列出的 6 个文件逐一核对处理方式（3 modify、3 create）；未触碰任何 Flyway 迁移；三处预先标记的偏离（`jobs.enabled=true`+`poll-interval`、四个用例的 `await().untilAsserted()`、静态 `ObjectMapper MAPPER`）逐一独立核实为对齐既有惯例的修正而非回归或范围蔓延；顺带自行发现并核实了第四处未预先标记但同样正确的偏离（Boot 4.1 `AutoConfigureMockMvc` 导入路径）。8 条 Global Constraints（迁移纪律、Jackson 3、`pollOnce()` 异步语义、`JobPoller` 测试接线、条件 UPDATE 原子跃迁 + `dedup_key` 两道防线、失败分两类分别处理、重名不覆盖、`UploadAssembler` 零 `@Transactional`）全部对着 diff 核实通过。无 Critical/Important，Task quality: Approved。2 处 deferred Minor：`aHashMismatchFailsTheSessionAndDoesNotRetryForever` 用例结尾对 job 行 `status == 'SUCCEEDED'` 的断言紧跟在前一个 `await().untilAsserted()`（只等会话状态变 `FAILED`）之后单独跑一次未重试的查询，`JobScheduler.execute()` 提交处理器工作与提交 `claimService.recordSuccess(...)` 是两次独立事务，理论上仍有一个很窄的同线程竞态窗口（636 个测试里没有 flake 过，风险低，未修）；`UploadAssembler.assemble()` 在大小/哈希不符分支里先 `markFailed` 再 `storage.deleteSession`（brief 原文如此，plan-mandated），若 `deleteSession` 本身抛 `IOException` 会传给任务表重试，但会话已经终态 `FAILED`，重试会经 `status != ASSEMBLING` 短路退出而不清理残留的临时目录，影响面是遗留临时文件而非数据丢失，未修。
- **SDD 记录**：`.superpowers/sdd/2026-08-17-06-search-and-upload/progress.md`，Task 12 brief/report/review diff 均在该目录。

**Task 13 完成（2026-08-25）——计划 06 全部 13 个任务执行完毕**

- **性质**：纯验收与记录任务，不改变前 12 个任务定下的任何契约，未新增或修改任何生产代码、迁移或测试。
- **全量验证**：`mvn -B -ntp verify` → **636 tests，0 failures，0 errors，2 skipped，BUILD SUCCESS**，与 Task 12 交付时的数字一致——收尾阶段没有引入任何回归。2 个 skipped 仍是既有环境门控用例（本机无 ffprobe、Windows 无符号链接权限）。
- **迁移验证**：`mvn -B -ntp test -Dtest=FlywayMigrationTest` → 3/3，确认 V1→V16 在全新 PostgreSQL 17 容器上依次执行成功，`V12` 的双参数 `to_tsvector('english', …)` 生成列与 `V15`/`V16` 对 V5/V6/V8 已建表的引用均无顺序问题。
- **产物**：新增 `docs/adr/ADR-006-中文搜索的真实边界.md`（`%` 操作符不能当子串谓词的实测依据、两字中文查询退化为全表扫描的边界、分层排序 vs 加权求和的理由）与 `docs/adr/ADR-007-采样哈希与秒传的边界.md`（秒传复用改名检测哈希算法的三条边界：漏中段、只在库内生效、候选上限 8 会让秒传"漏"）；新增 `docs/walkthrough/06-检索与上传.md`，覆盖计划正文 Step 5 要求的十个点（两条搜索路径、分层排序、LIKE 转义、标签的第三次域分区强制、用户态四表为何不合并、分享链接的多态外键替代方案、能力令牌与 HMAC 票据、`/api/share/**` 与 `/api/shares` 的路径匹配、分片上传绕开 multipart、秒传三条边界），并附客户端计算采样哈希的 20 行 JS 与自验证 SQL/PowerShell 命令。
- **本文件的其余更新**：§1 计划表 06 行标记为已写已执行（待合入 `main`）、07 标记为下一份；§5 已知遗留追加 G20–G23（秒传边界、上传临时目录未清理、分享链接无限流、搜索无分页）。§2 的迁移编号/任务类型行、§3 的三条实测事实、§6 的 ADR-006/007 归属，均已在 Task 1–12 执行期间随任务逐步写入，本任务核对后确认无需改动。
- **当时未合并**：Task 1–13 完成时改动仍留在 `D:\MyMedia-6` 分支 `plan06-2026-08-17-06-search-and-upload`。**已于 2026-08-25 合并，见 §12。**
- **SDD 记录**：`.superpowers/sdd/2026-08-17-06-search-and-upload/progress.md`。

## 12. 计划 06 最终审查与合并（2026-08-25）

- **最终审查**：合并前在 `D:\MyMedia-6` 独立跑 `mvn -q test`（无 `-DskipTests` 参数分支干扰），**exit 0，无 `[ERROR]`/`FAILURE!` 标记**，与 Task 13 交付时的 636/0/0/2 基线一致。另派发一次独立的 `/code-review high main`（并行 8 个 finder 角度 + 逐条读码验证，对 95 个变更文件、约 7570 行插入做整体扫描，覆盖前 13 个任务各自 task-level review 之外的**跨任务**视角），发现 10 条 Minor，**0 条 Critical/Important**：
  1. **新发现、值得记账的一条**：`ImageFavoriteService.listNodes` / `VideoFavoriteService.listItems`（Task 7）在 `catalogService.findByIds(...)` 之后不重新校验当前库访问权，与同一份 diff 里 `TagLinkController.targets()`（Task 6）在同一模式下会 `.filter(accessService.canAccess(...))` 不一致——库访问权被撤销后，收藏列表仍会吐出该条目的标题/封面等信息。这与 §9 记录在案的 `continueWatching`/`continueReading` 不按库访问权过滤是**同一类**已被本项目多次接受为非阻塞 Minor 的缺口，本次按同一先例处理：不阻塞合并，记入 §5 G24。
  2. 其余 9 条均为已知模式的重复实例，均不新增行动项：`video`/`image` 两个域的收藏、搜索、分享控制器逐字对称重复（Task 7/2-3/8-9 的收藏/PR 记录里已明确这是刻意取舍——抽公共模块会引入"目标类型"多态列，本项目一直在避免）；`X-Share-Ticket` 头常量在两个控制器里各自定义（Task 9 交接已记为 deferred Minor）；`UploadAssembler` 同名并发写入的极窄竞态（自愈：失败后按 job 退避重试，不会覆盖或丢数据）；`SafeFileName.truncate` 理论上可能切断代理对（补充字符平面文件名，概率极低）；`UploadSessionService` 分片数计算在管理员把 `max-size`/`chunk-size` 配到超出 `int` 范围时会溢出（需要人为极端误配置）；`ScannedFileHashService`/`RelocationDetector` 各自包一层同构的哈希异常处理（两处逻辑现在完全一致，尚未出现分叉）；`UploadSession.instant` 与 `scannedFileId != null` 语义冗余（当前唯一写入点保证同步）；`TagLinkStore`/`TagLinkController` 用二元三目而非穷尽 `switch` 路由 `LibraryDomain`（`LibraryDomain` 目前只有两个取值，安全，但不会在编译期防住第三个取值）。
- **裁决**：因为 (a) 无 Critical/Important，(b) 全量测试在合并前后各跑一次都是绿的，(c) 唯一新发现的问题与项目已经三次确认接受的"列表不重新校验访问权"是同一类别、同一严重度，**不为此再开一轮修复**，直接按用户指示合并。
- **合并**：`git merge --no-ff plan06-2026-08-17-06-search-and-upload`，合并提交 `cb691cb`，tag `plan-06-search_and_upload`。合并后在 `main`（`D:\MyMedia`）重跑 `mvn -q test`：**exit 0**，与合并前一致，无回归。已推送 `origin/main` 与该 tag。
- **收尾**：计划 06 的 worktree `D:\MyMedia-6` 与分支 `plan06-2026-08-17-06-search-and-upload` 按 §10 立下的规矩，删前先把 SDD 工作区归档到 `D:\MyMedia\.superpowers\sdd\2026-08-17-06-search-and-upload`，再删除。
- **下一步**：计划 01–06（P0–P11）全部已执行并合入 `main`。**计划 07（前端，P12）已于 2026-08-26 写完**，设计定稿见 §13，正文见 `2026-08-17-07-frontend.md`。新窗口先读本文件的 §2 / §3 / §5 / §13，再读计划 07 开始执行；从 `main` 创建独立 worktree，**本计划不新增 Flyway 迁移**。

---

## 13. 计划 07 的设计定稿（**已落进 `2026-08-17-07-frontend.md`**）

> 与 §4 给计划 05 留的痕迹同样用途：记录「设计先定稿、再展开成计划」这一步。
> **执行时以计划 07 为准**，那份文件里的细节多得多。

### 13.1 用户拍板的四个决定（写计划时定的，执行时别再翻案）

| 决定点 | 结论 |
|---|---|
| 技术栈 | **Vue 3 + Vite + TypeScript**（spec §8 原定，用户确认）。**不引 UI 组件库、不引 CSS 框架、不引 axios**——与后端「不引入无法解释的中间件」同一条理由 |
| `<video>`/`<img>` 带不了 Basic 头 | **短期签名票据走 query 参数**。保住 ADR-002 的无状态与零 CSRF 面，复用 `ShareTicket` 的机制 |
| 界面范围 | **全量**（消费 + 管理）。后端每一项能力都有界面能演示 |
| 构建集成 | **`frontend-maven-plugin` 绑进 `mvn` 生命周期**，`npm ci` → `npm run test` → `npm run build` → 产物拷进 `target/classes/static/` |
| 测试策略 | **Vitest 只测纯逻辑**（票据缓存、阅读器状态机、节流、雪碧图坐标、分片推算、时长格式化），组件渲染不测；新增后端端点照旧写 MockMvc + Testcontainers |

### 13.2 实测版本矩阵（**前端版的「`4.1.0.RELEASE` 陷阱」在这里**）

| 组件 | 版本 | 验证方式 |
|---|---|---|
| frontend-maven-plugin | `2.0.2` | 本机实跑：下载 Node → `npm ci` → `npm run build` → 产物落 `target/classes/static/`，全链路 exit 0 |
| Node | `v24.19.0`（Krypton LTS） | 同上。**自带 npm 11.17.0，不要配 `<npmVersion>`** |
| Vue / vue-router / Pinia | `3.5.41` / `5.2.0` / `4.0.3` | 实装，无 peer 冲突 |
| Vite / @vitejs/plugin-vue / Vitest | `8.2.2` / `6.0.8` / `4.1.11` | `vite build` 与 `vitest run` 均 exit 0 |
| **TypeScript** | **`6.0.3`——不是 `latest`** | 见下 |
| vue-tsc | `3.3.11` | 配 TS 5.9.3 ✅ / 6.0.3 ✅ / **7.0.2 ❌** |

- **`typescript@latest` 现在是 `7.0.2`（Go 原生重写版），它的包里不再导出 `./lib/tsc`**，而 `vue-tsc@3.3.11` 正是靠 `require.resolve('typescript/lib/tsc')` 启动 → `ERR_PACKAGE_PATH_NOT_EXPORTED`，直接崩。`vue-tsc` 的 peer 只写 `typescript: ">=5.0.0"`，**声明是过宽的，拦不住你**。三个版本各实跑过一遍。
- **`npx vue-tsc --noEmit 2>&1 | tail` 会把退出码变成 `tail` 的 0，类型错误被静默吞掉。** 判定必须先重定向到文件再读 `$?`。写计划时第一次就被它骗过。

### 13.3 六个后端缺口（全部由 A 段 Task 1–4 补掉）

| # | 缺口 | 补法 |
|---|---|---|
| 1 | `<video>`/`<img>` 带不了 Basic 头 | `shared.HmacSigner` + `user.MediaTicketService` + 路径白名单过滤器 |
| 2 | `ItemSummary` / `NodeSummary` 无 `coverAssetId`（只有搜索命中有），列表页渲染不出封面 | 两个 DTO 各追加一个分量；顺带追加 `libraryId` 以便按库分段 |
| 3 | 无 `/api/auth/me`（`/api/auth/**` 在 `SecurityConfig` 里 `permitAll` 却一个控制器都没有，是**死配置**） | `user.AuthController`；同时删掉那条死规则、换掉 401 入口点去掉 `WWW-Authenticate` 挑战头 |
| 4 | 雪碧图无查询入口——`/api/assets/{id}` 按 id 取，而前端只有 `videoFileId` | `preview.web.VideoPreviewController`。**只能放在 `preview`**：`video` 不许依赖 `preview` |
| 5 | `web` 无静态托管与 history fallback | `SpaWebConfig` + `PathResourceResolver` 兜底 |
| 6 | **继续观看只返回 `{fileId, positionSeconds, durationSeconds, completed}`**——没有标题、没有封面、连 `itemId` 都没有，卡片渲染不出来 | 查询下推富化；继续阅读对称处理 |

**计划 07 不需要任何 Flyway 迁移**，V17 完整留给计划 08。

### 13.4 媒体票据（ADR-008）

- `shared.HmacSigner` 提供签名原语，`library.ShareTicket` 重构成委托它（**行为一个字节不变，既有测试原样绿**），`user.MediaTicketService` 复用同一段。这是 **G8 把 `SampledHash` 提升到 `shared` 的同一个先例**：第二个消费者出现时才提取。`user` 因此仍然只依赖 `shared`。
- 形状 `<userId>.<expEpochSec>.<sig>`，TTL 15 分钟，前端过期前 30 秒自动续签。
- **不是给三个控制器加 `@RequestParam`，而是一个装在 `BasicAuthenticationFilter` 之前的过滤器**——27 个既有控制器一行不改。
- **白名单只有三条路径**：`/api/video/stream/**`、`/api/image/page/**`、`/api/assets/**`。URL 里的凭证会漏进日志、`Referer`、浏览器历史；把它能干的事限制在「看内容」，泄漏的上界就被钉死。
- **密钥留空时每次启动随机是正确默认**——与 `ShareTicket` 相反（那里随机会让访客重输密码，是 G14）。同一个算法，两种生命周期，两种默认值。

### 13.5 视觉体系

**主题：两个域是两种物理介质。视频发光，图片反光。** 这条区分不是修辞，它决定了每一个 CSS 令牌：

- 视频卡片 `box-shadow` 是**彩色外发光**，图片卡片是**黑色落影**（`--elevation` 在两域各自定义）。
- 视频域底色偏冷（`#08090E`，影院的黑被屏幕溢光染蓝），图片域偏暖（`#12100D`，台灯下纸堆的阴影）。
- 强调色：视频 `#FFB020`（放映灯的琥珀），图片 `#C0483A`（印泥的朱，不是霓虹的红）。两者色相接近，**真正的区分在行为**——一个发光，一个盖章。
- **全应用唯一的亮面是 `#F2EDE3` 的纸色**，只在阅读器里满幅出现。

**实现方式**：CSS 自定义属性 + `[data-domain="video"]` / `[data-domain="image"]` 属性选择器。外壳按路由 `meta.domain` 给 `<body>` 打标；全局搜索页把两段各自包一层 `data-domain`，**同一屏上两个域看起来就是两种东西**——这是整套体系最能说明问题的一屏。

**两个签名元素，各自是一处后端投入的可见回报：**

1. **视频域——进度条上的雪碧图预览帧。** 计划 05 花一整个任务生成 100 帧雪碧图与 WebVTT，悬停浮出对应时刻的画面。零网络请求（一张大图 + `background-position`）。
2. **图片域——每张书卡左缘 4px 的书脊。** 一道亮—暗—亮的渐变，让封面读起来是有厚度的册子而不是缩略图。两行 CSS 表达了整套 `image_node` 树的核心主张：图片域的单位是「一本」。

**两处刻意的冒险**：
- 阅读器**完全抛弃暗色外壳**，满幅铺纸色、chrome 全隐。别的阅读器都留着暗框。理由是实的：暗框围亮页会产生光晕，长时间阅读难受；而且让「换了一个域」被身体直接感知。
- 图片域用 **CSS 多列瀑布流**（封面按原比例不裁切），视频域用**等高 16:9 网格**。视频封面天然 16:9，裁切无损；漫画封面比例天差地别，裁成统一会把标题和角色脸切掉。这个不对称与后端「视频域语义强、图片域组织自由」是同一条判断。

**字体**：Archivo（视频标题）/ Literata（图片标题）/ IBM Plex Sans（正文）/ IBM Plex Mono（全部数字）。**拉丁字形自托管，CJK 走系统字体栈**——完整 CJK 5–15 MB，对一个要求离线可用的自托管服务不可接受；**绝不 `<link>` Google Fonts**，离线时会静默退化、整套排版崩相。

### 13.6 19 个任务的分段

| 段 | 任务 | 内容 |
|---|---|---|
| **A 地基** | 1–4 | 认证补口 / 票据过滤器 / DTO 与端点补口（关 G24、G13）/ SPA 托管。**Task 4 完成是一个可独立验收的分界点**：后端补口全绿、尚无一行前端代码 |
| **B 骨架** | 5–7 | 脚手架与构建集成 / 设计令牌与外壳 / API 客户端·认证态·票据·登录页 |
| **C 视频域** | 8–11 | 首页 / 条目详情与剧集 / **播放器**（雪碧图 + 节流上报）/ 目录树与搜索 |
| **D 图片域** | 12–14 | 首页与书卡 / 节点浏览与阅读模式 / **阅读器**（三模式 + 双向） |
| **E 跨域与管理** | 15–19 | 全局分区搜索·收藏·标签 / 分享页 / 媒体库管理 / 上传·刮削确认·元数据编辑 / 收尾（ADR-008 + 讲解文档） |

### 13.7 刻意不做（写进计划的 Self-Review，执行时别当成遗漏）

- **带密码的分享链接不支持媒体流播放**。分享票据是 `X-Share-Ticket` 请求头，`<video src>` 同样带不了；而给它也开一条 query 入口，等于把凭证挪到一个**本来就被到处转发**的 URL 上——转发一次密码保护就废了。带密码链接解锁后只显示条目信息与说明。**这是取舍，不是遗漏。**
- 组件渲染测试。缺陷高发区是阅读器状态机与分片推算，它们完全不需要 DOM。
- spec §7.4 的「按 Accept 转 WebP」与「服务端预读下一页」——计划 04 已明确不做；前端做客户端预读覆盖了后者的实际收益。

### 13.8 执行时要当心的两处跨任务回改

计划里已显式标注，但**若用 subagent-driven 执行，主 agent 必须在派发 Task 3 之前就把它们并进 Task 3 的 brief**，否则会变成两次返工：

1. Task 8 与 Task 12 都需要 `ItemSummary` / `NodeSummary` 带 `libraryId`（用于按库分段），而这两个 DTO 在 Task 3 修改。
2. Task 16（分享页）要求 Task 10 的播放器与 Task 14 的阅读器把「页地址构造函数」做成 prop（`srcFor: (fileId: number) => Promise<string>`），而不是自己拼 `/api/video/stream/...`。回头改比一开始留难得多。

## 14. 计划 07 执行交接（A+B 段，2026-08-26）

- **工作区与状态**：`D:\MyMedia-7`，分支 `plan07-2026-08-17-07-frontend`，base `858da38`（计划 06 合并提交之后）。Task 1–7 已完成并逐任务 review clean，**尚未合入 `main`**。HEAD `77451e0`。SDD ledger：`.superpowers/sdd/2026-08-17-07-frontend/progress.md`（含全部 preflight 扫描表、Ruling 明细、每任务 brief/report/review 包）。
- **A 地基段（Task 1–4，2026-08-26 上午）**：认证补口、媒体票据机制、DTO 与端点补口、SPA 托管。提交链 `b4a1a68`→`50f3488`（Task 1）→`91cc936`（Task 2）→`e025ab1`（Task 2 fix round 1）→`bc319da`（Task 3）→`15e2561`（Task 4）。合并后 `mvn verify`：**681 tests，0 failures，0 errors，2 skipped，BUILD SUCCESS**（基线 636 + 45）。四个任务全部独立 review clean，Task 2 经一轮 fix（票据过滤器漏了停用账号检查、且没测过「Basic 优先于票据」这条机制）后收敛。preflight 阶段裁定 R1–R6（DTO 补 `libraryId` 的位置、Files 清单补漏、G24 只关四处不动 `VideoProgressService.record`、`SpaWebConfig` 拆两个 resource handler 避免 SPA 外壳被缓存一年、404 用例补认证、`register()` 实际是三参不是四参）。**A 段末尾派发过一次全分支 858da38..15e2561 的 final review，结果在会话中断前丢失，未重新派发**——19 个任务全做完后的 Final Review 会用完整 diff 重新覆盖这段，不单独补。
- **B 骨架段（Task 5–7，2026-08-26 下午）**：前端脚手架与 Maven 构建集成、设计令牌与自托管字体与应用外壳、API 客户端与登录页。提交链 `3d3910b`（Task 5）→`4894835`（Task 5 fix round 1）→`2f7305b`（Task 6）→`e2f4a39`（Task 7）→`77451e0`（Task 7 fix round 1）。`frontend/` 首次出现：Vite 8.2.2 + Vue 3.5.41 + TypeScript **6.0.3（锁死，不能用 latest）** + Vitest 4.1.11，`frontend-maven-plugin` 绑进 `generate-resources` 阶段。Task 7 完成时 Vitest **16/16**（brief 原文写「13」是算术错误，已订正）、`vue-tsc -b --force`/`vite build` 均 exit=0。控制器两次亲自起 Postgres + `spring-boot:run` 做端到端验证（Task 5 的 SPA 托管、Task 7 的后端契约字段），因为沙箱里 Docker Desktop 一开始没启动，`Start-Process` 拉起来即可、不需要管理员权限。preflight 裁定 R7–R10：
  - **R7**：`Cover.vue`（Task 6）提前引用了 Task 7 才建的 `api/media.ts`，让 Task 6 先建一个占位版（只有 `assetUrl` 返回不带票据的 URL），Task 7 整份覆盖重写，签名不变。
  - **R8**：brief 说 `client.ts` 该做「401 清凭证/跳登录」但没给代码也没定接口，补了 `setUnauthorizedHandler(handler)` 的具体形状，`main.ts` 里接线。
  - **R9**：字体文件（Archivo/Literata/IBM Plex Sans/IBM Plex Mono）来源用 `npm install --no-save @fontsource/*`，取 `-latin-` 变体改名，不进 `package.json`。
  - **R10**：订正 brief 一处算术错误（预期测试数「13」应为「16」）。
  - Task 6 实现者自己发现并处理了一个 R7 之外的同类转发引用：router 逐字引用的 18 个视图文件当时都不存在，`vite build`（Rollup）解析动态 `import()` 需要真实文件，建了 18 个最小占位 `.vue`（无 `<script>`），Task 7 只替换其中的 `LoginView.vue`。
  - **Task 7 的 fix round 是一个真实 bug**：R8 要求 `main.ts` 顶层同步 `useAuthStore()`，连带 eager import 了 `media.ts`；`media.ts` 模块级签票 prefetch 在匿名启动时会打一次注定 401 的请求，而 `client.ts` 的 401 排除名单只挡了 `/api/auth/me`，于是触发 `unauthorizedHandler` 做一次「裸」`router.push({ name: 'login' })`，把 `router.beforeEach` 刚设好的 `redirect` 查询参数覆盖掉——净效果是深链登录后总跳回首页而不是原目标页。修复：给 prefetch 加 `hasCredential()` 门槛（`try/catch` 包裹，因为裸 `if` 在 Vitest 的 node 环境下会同步抛出 `sessionStorage is not defined` 崩测试）。**残余边界未修**：sessionStorage 里存在**已失效**（非"完全没有"）凭证时，同一条竞态理论上仍可能发生；彻底堵住需要让 `unauthorizedHandler` 感知当前路由已有的 `redirect`，超出 Task 7 范围。
- **本次会话额外核实/清理**：`mvn -B -ntp clean verify`（全量，含 Testcontainers 集成测试，不带 `-DskipFrontend`）在 A+B 段完成后重跑一次，结果见下方「全量验证」；`docs/superpowers/plans/` 下两份计划文件里各有几处历史遗留的 `</content>`/`</invoke>` 杂散标签（早于本次会话，推测是先前写计划文件时的粘贴事故），顺手清理掉，不影响任何执行内容。
- **遗留 Minor（不阻塞，留给后续判断）**：
  1. Task 6：`favicon.svg` 用了视频域强调色而非中性壳色；`fonts.css` 的 `unicode-range` 在 8 条 `@font-face` 规则里结构性重复（CSS 限制，非疏漏）。
  2. Task 7：`media.ts` 里 `shouldPrefetch` 的 `catch` 没打日志，与旁边 fetch-failure 的 catch 不对称。
  3. Task 7：上面提到的"已失效凭证"残余竞态边界。
- **下一步（A+B 段完成时的记录，已被 §15 取代）**：见 §15——C 段已经执行完。

## 15. 计划 07 执行交接（C 视频域段，2026-08-27）

- **工作区与状态**：`D:\MyMedia-7`，分支 `plan07-2026-08-17-07-frontend` 不变。BASE `5671d0e`（A+B 段收尾提交之后）。Task 8–11 已完成并逐任务 review clean，**尚未合入 `main`**。HEAD `78a1e5e`。SDD ledger：`.superpowers/sdd/2026-08-17-07-frontend/progress.md`（含本段的 preflight 扫描表、Ruling 明细、每任务 brief/report/review 包）。
- **C 视频域段（Task 8–11）**：视频首页与继续观看、条目详情与剧集列表、播放器（雪碧图预览 + 节流上报）、目录树浏览与搜索。提交链 `3bbbc27`（Task 8）→`981dfef`（Task 9）→`956a174`（Task 9 fix round 1）→`ec1c307`（Task 10）→`772fbbd`（Task 10 fix round 1）→`d7f54bc`（Task 11）→`78a1e5e`（Task 11 fix round 1）。四个任务全部独立 review，Task 9/10/11 各经过一轮 fix loop 后收敛，Task 8 一次通过；全部 Spec ✅ / Quality Approved，无 Critical/Important 悬而未决。本段结束后跑了一次不带 skip 标志的 `mvn -B -ntp clean verify`：**681 后端测试、0 failures、0 errors、2 skipped** + 前端 `Test Files 5 passed / Tests 29 passed`，`BUILD SUCCESS`。
- **本段发现并修复的两处真实后端契约缺口**（均遵循 G8/Task 1 的先例——"第二个消费者出现时才提取"——都是纯加法、无迁移、由 reviewer 独立核实必要性与测试有效性）：
  1. **Task 9**：`GET /api/video/items/{id}` 的 `VideoCatalogDto.FileSummary` 原本没有 `groupId`，而 `episodeIndex` 跨季重复（S1E01/S2E01 都是 1），前端没法把文件正确分进 `GROUPED` 结构的哪一节。加了 `Long groupId`（从既有的 `VideoFile.getGroupId()` 取），同步补了一条真实断言的单元测试。
  2. **Task 10**：`GET /api/preview/video/{videoFileId}` 的 `VideoPreviewController.VideoPreviewView` 原本没有 `itemId`，导致播放器的"上一集/下一集"退化成只对已经播放过（在 `continueWatching()` 里有记录）的文件生效——对"第一次看"这个最常见场景实际不可用。加了 `Long itemId`（从控制器里已经查出的 `item` 变量取），并把前端改成直接吃这个字段而不是反查 `continueWatching()`；控制器亲自拉起 Docker 跑了 `VideoPreviewControllerTest`（4/4 绿）交给 re-reviewer 核实，不是只信 `mvn -o compile`。
- **视觉体系落地情况**：视频域"发光"卡片（`VideoCard.vue` 的 `--elevation` 彩色外发光）、雪碧图悬停预览（`ScrubBar.vue` 的 `background-position` 零网络请求换帧）、continueWatching 横向滚动条、目录浏览次要视图定位，均按 §13.5 的设计定稿落地。
- **遗留 Minor（不阻塞，留给后续任务或最终 whole-branch review 判断）**：
  1. `VideoCard.vue`/`ContinueRow.vue` 的卡片样式与进度计算逻辑重复；`PlayerView.vue` 与 `EpisodeList.vue` 的排序逻辑也重复（含同一个 `Infinity-Infinity=NaN` 既有模式，非本段引入）。
  2. `TagPicker.vue` 的建议下拉对键盘用户不可达（Tab 会跳过建议项，回车创建仍可键盘操作）；「有效天数」输入无客户端夹逼，交给后端 `@Min`/`@Max` 兜底。
  3. `SearchBox.vue` 会在 `/login` 页也渲染并静默 401（既有 AppShell/router 结构问题，非本段引入）；建议下拉"最多 5 条"实际是每域各 5 条（最多可能 10 条），措辞与实现有出入但非功能缺陷。
  4. `resolveSiblingEpisodes` 每次加载 `PlayerView` 都重新拉一次 `itemDetail()`（`ItemDetailView` 往往已经有这份数据）；`VideoBrowseView.vue` 的加载骨架屏在两处模板位置重复。
- **下一步**：D 图片域（Task 12–14：首页与书卡、节点浏览与阅读模式、阅读器）。新窗口在同一个 worktree `D:\MyMedia-7`（分支不变）里继续，先读本文件 §2/§3/§5/§13/§14/§15，再读 `.superpowers/sdd/2026-08-17-07-frontend/progress.md` 恢复 ledger 状态，接着派发 Task 12。图片域的视觉体系（书卡书脊、CSS 多列瀑布流、阅读器满幅纸色）见 §13.5，执行前建议重新过一遍。

## 16. 计划 07 执行交接（D 图片域段，2026-08-27）

- **工作区与状态**：`D:\MyMedia-7`，分支 `plan07-2026-08-17-07-frontend` 不变。BASE `78a1e5e`（C 段末尾）。Task 12–14 已完成并逐任务 review clean，**尚未合入 `main`**。HEAD `0fdfd16`。SDD ledger：`.superpowers/sdd/2026-08-17-07-frontend/progress.md`（含本段的 preflight 扫描表、Ruling R16–R20 明细、每任务 brief/report/review 包）。
- **D 图片域段（Task 12–14）**：图片首页与书卡/瀑布流网格、节点浏览与面包屑与阅读模式覆盖与图片搜索、阅读器（三种模式 + 双向翻页）。提交链 `4f11478`（Task 12）→`7e3d6e9`（Task 12 fix round 1）→`da649fc`（Task 13）→`6a18bdd`（Task 13 R20 追加）→`caeddbf`（Task 13 fix round 1）→`0fdfd16`（Task 14）。三个任务全部独立 review，Task 12/13 各经过一轮 fix loop 后收敛，Task 14 一次通过；全部 Spec ✅ / Quality Approved，无 Critical/Important 悬而未决。段末 `vue-tsc -b --force`/`vite build`/`npx vitest run`（6 files/44 tests）均 exit=0。
- **本段的五条 preflight Ruling（R16–R20，均已核实落地）**：R16（`api/image.ts` 的 `browse` 签名改为必填 `libraryId`，与视频域 Task 11 R14 同一类缺口）；R17（`NodeBrowseView.vue` 先 `nodeDetail` 后 `browse` 的顺序装载，banner 用 `directPageCount`，`breadcrumb` 已含当前节点自己）；R18（修正计划文本的过时前提——`ImageNodeDto.NodeSummary` 的 `libraryId` 在 A 段 Task 3 已经加过，本段不改后端）；R19（`BookCard.vue` 的 `node` prop 窄化成本地类型，供 Task 13 的搜索结果映射，与视频域 Task 11 R11 同构）；R20（把 `SearchBox.vue` 的 Ctrl+Enter 域内搜索开关补上图片域分支，纳入 Task 13 范围，因为该任务正是让 `image-search` 从占位变成真实页面的任务）。
- **本段执行中处理的两处非 preflight 缺口**：Task 12 实现者独立发现并修正了与 R16 同结论的 `browse()` 签名问题（两边核对一致）；Task 12 review 抓到一处按钮嵌套在 `RouterLink` 里的无障碍缺陷（HTML5 不允许交互元素互相嵌套），Task 13 review 抓到阅读模式切换失败时下拉框未真正回退且无可见提示——两处均经一轮 fix loop 收敛。
- **视觉体系落地情况**（计划 §13.5 定稿的图片域两个签名点，均由对应任务的 review 独立核实）：书卡投影（`BookCard.vue` 的黑色落影 `--elevation`，与视频域彩色发光对照）、书脊签名元素（`.spine` 4px 竖向渐变）、CSS 多列瀑布流（不裁切封面比例，区别于视频域的等高网格）、阅读器满幅纸色 `--page`/`--page-ink` 完全抛弃暗色壳（计划点名的"第二处刻意冒险"，`AppShell.vue` 的 `meta.bare` 机制原样支持，未改代码）。
- **遗留 Minor（不阻塞，留给后续任务或最终 whole-branch review 判断）**：
  1. Task 12：`BookCardNode`/`GridNode` 两个结构相同的接口独立定义（未共享 import）；hover 阴影用字面量颜色值而非令牌；`.spine` 的 `z-index: 1` 可能不必要。
  2. Task 13：`.header-row` 无 `flex-wrap`，窄屏下面包屑 + 阅读模式下拉可能横向溢出；`Breadcrumb.vue` 的"…"展开按钮无 `aria-label`。
  3. Task 14：`ReaderChrome.vue` 声明了 `next`/`prev` emit 但没有按钮触发（死接线，brief 也未要求）；`PageView.vue` 的 `<img>` 无 `@error` 处理，图片真正加载失败时占位会永远停留且无提示；`ReaderView.vue` 内联的错误信息归一化逻辑与 `ErrorState.vue` 重复；内联错误态缺少次要提示行；页面用数组下标而非 `ImagePageSummary.pageIndex` 取值（大概率安全但略隐式）。
  4. 手工浏览器视觉验收（Task 12 的暖色/书脊/投影三条判据、Task 14 的翻页过渡观感与占位框贴合度）全程未做——本环境无法起浏览器人工点击，均已在代码层面独立核实设计意图落地，建议后续有条件时补一次真实点击验收。
- **§13.8 第 2 条的一处待办**（非本段范围，记录供 Task 16 预检时参考）：视频播放器与图片阅读器实际都没有做成"地址构造函数是 prop"的形状——`VideoPlayer.vue` 接收已解析好的 `src: string`，`PageView.vue` 同样自行 `await mediaUrl(...)`。Task 16（分享页）要复用两者但走不同的地址解析（`X-Share-Ticket` 而非登录票据），派发前需要重新评估，届时可能要对已完成的 Task 10/14 做一次回改。
- **下一步（D 段完成时的记录，已被 §17 取代）**：见 §17——Task 15 已经执行完。

## 17. 计划 07 执行交接（E 段 Task 15，2026-08-27）

- **工作区与状态**：`D:\MyMedia-7`，分支 `plan07-2026-08-17-07-frontend` 不变。BASE `baded4d`（D 图片域段交接提交之后）。Task 15 已完成并 review clean，**尚未合入 `main`**。HEAD `cc09a34`。SDD ledger：`.superpowers/sdd/2026-08-17-07-frontend/progress.md`（含本次的 preflight 扫描表、Ruling R21–R24 明细、brief/report/review 包）。**用户本次会话明确要求只做 Task 15**，Task 16–19 未派发。
- **Task 15（全局分区搜索、收藏与标签浏览）**：提交 `cc09a34`（单提交，前端-only）。`vue-tsc -b --force`/`vite build`/`npx vitest run`（44/44，未新增测试，符合"组件渲染不写测试"的既定约定）均 exit=0。一次通过 review，无 fix loop：Spec ✅ / Quality Approved / 0 Critical / 0 Important，3 条 Minor（见下）。
- **本段的四条 preflight Ruling（R21–R24，均已核实落地）**：
  1. **R21**——`favorites.ts` 的 `listFavorites` 从占位的 `{id:number}[]` 改成按 `kind` 字面量重载的真实类型（`VideoFavoriteEntry`/`ImageFavoriteEntry`，新增进 `types.ts`）。不改后端：`GET /api/video|image/favorites` 早就在裸序列化 `VideoItem`/`ImageNode` 实体，字段本来就在响应体里，只是前端类型声明过窄——文件自己的注释当初就点名了"下一个展示收藏列表的任务该做这件事"。
  2. **R22**——`TagView.vue` 把 `TagDto.TaggedTarget(id,title,coverAssetId)` 映射进 `BookCard` 的 `node` prop 时，`readable` 与 `browsable` **都**填 `false`（不是像 Task 13 的 `ImageSearchHit` 映射那样只填假 `browsable`）——因为 `TaggedTarget` 根本没有 `readable` 字段可用（后端注释："只需要够画一张卡片"），双假值让 `BookCard` 的路由分支永远落到安全的浏览页，且不会渲染编造的"0 页/0 项"。
  3. **R23**——`TagView.vue` 用两个常驻分区（与 SearchView/FavoritesView 同一设计语言）：并行拉两个域的 `listTags`，在结果里按 `id` 找出当前标签所属的域与名字（`TagDto.Response` 自带 `domain` 字段，不需要新端点——`TagController` 本来就没有单标签查询接口）；两个域的 chip 行始终都渲染，只有当前标签所属域在 chip 行下方渲染 `itemsOfTag` 结果；id 在两个域都找不到时渲染独立于网络错误分支的"标签不存在"提示。
  4. **R24**——让 Task 15 新建的三个页面真正可达（比照 C 段 R13、D 段 R20 的"交付物必须可达"先例，`DomainNav.vue` 明确禁止加第三个顶层入口，所以只能走次要位置）：`VideoHomeView.vue`/`ImageHomeView.vue` 各加一条「我的收藏」链接；`TagPicker.vue` 的已选标签 chip 名字包一层指向 `/tags/:id` 的 `RouterLink`，与既有的移除按钮保持兄弟节点（不嵌套交互元素，对应 D 段 Task 12 review 抓到过的同类问题）。
- **两处审查独立核实过的实现者判断调用**（均成立，非搪塞）：(a) Files 清单写了要改 `tags.ts`，但 `listTags`/`itemsOfTag` 的既有签名已经满足全部三个新视图，本次实际未改这个文件；(b) TagView 的"标签不存在"没有复用 `<ErrorState>` 组件本体（该组件的固定文案"加载失败…检查服务端日志"套在"标签已被删除"这个合法的非网络错误上语义不通），只是视觉上仿照它的间距与 `role="alert"`。
- **遗留 Minor（不阻塞，留给后续任务或最终 whole-branch review 判断）**：SearchView/FavoritesView 的分区标题/网格/骨架屏 CSS 逐字重复约 90 行（与既有 VideoSearchView/ImageSearchView 的"每个 SFC 自带样式"惯例一致，非新增坏味道）；TagView 的"标签不存在"块与 `ErrorState.vue` 的 CSS 结构重复约 15 行（内容分叉有理由，样式可以通过给 `ErrorState` 加可选 `title`/`hint` override 来避免）；TagView 的"标签不存在"分支隐藏了本可以让用户跳到别的有效标签的 chip 行（R23 未要求，体验层面可以更好）。
- **下一步（Task 15 完成时的记录，已被 §18 取代）**：见 §18——Task 16–19 已经全部执行完，计划 07 的 19 个任务已经全部完成。

## 18. 计划 07 执行交接（E 段 Task 16–19，全部完成，2026-08-27～28）

- **工作区与状态**：`D:\MyMedia-7`，分支 `plan07-2026-08-17-07-frontend` 不变。BASE `054ffbd`（Task 15 交接提交之后）。Task 16–19 全部完成并逐任务 review clean（Task 16/17/18 各经过一轮 fix loop 后收敛），**尚未合入 `main`**。HEAD `8deb25f`（Task 19 自己的提交）。SDD ledger：`.superpowers/sdd/2026-08-17-07-frontend/progress.md`（含本段的 preflight 扫描表、Ruling R25–R36 明细、每任务 brief/report/review 包）。**计划 07 的全部 19 个任务到此全部完成。**

### 提交链（Task 16–19）

| 任务 | 提交 |
|---|---|
| Task 16（免登录分享页） | `8215926` → `0008693`（fix round 1：分享页错误处理的两处双重标准） |
| Task 17（媒体库管理） | `8f1dc34` → `b2bd22f`（fix round 1：拆分两步建库的失败处理，避免建出无法删除的重复库） |
| Task 18（分片上传、刮削确认队列、元数据编辑） | `5344400`（后端：新增 `GET /api/scrape/queue`）→ `02b47f4`（前端实现）→ `95d04f2`（fix round 1：分片上传轮询合并阶段的错误处理） |
| Task 19（全量验证、ADR-008、讲解文档） | `8deb25f`（单提交，docs-only：新增 ADR-008、07-前端 walkthrough、README 三段更新、本节） |

### 真实的全量验证数字（Task 19 Step 1 实测，2026-08-28）

```
mvn -B -ntp clean verify
```
**BUILD SUCCESS**，`Tests run: 682, Failures: 0, Errors: 0, Skipped: 2`（对比计划 06 基线
636，净增 46）。这次全量重跑复现了数字的可靠性：682 比 A+B 段完成时的 681 多 1，
来自 Task 18 新增的 `GET /api/scrape/queue` 后端集成测试（`ScrapeQueueControllerTest`）；
Task 19 自身不改任何测试，682 是 C/D/E 五段累计增量的真实结果，不是重复计数。

```
cd frontend && npx vitest run
```
`Test Files 8 passed (8)`，`Tests 64 passed (64)`（对比 D 段完成时的 44，Task 15 未新增、
Task 16 未新增、Task 17 未新增、Task 18 新增 20 条——`chunker.test.ts` 11 + `sampledHash.test.ts`
9 = 20，44 + 20 = 64，逐任务净增数字对得上）。

### preflight 阶段裁定的 Ruling（R25–R36，均已核实落地，明细见 SDD ledger）

- **R25**：`VideoPlayer.vue` 不需要"地址构造函数"prop 回改（§13.8 第 2 条待办的最终结论）——无密码分享的媒体流端点对 `X-Share-Ticket` 完全可选，`<video src>` 可以直接拼一个不带票据的公开 URL；只有 `PageView.vue` 需要一个可选 `srcOverride` prop。`ShareView.vue` 不整体复用 `ReaderView.vue`/`ReaderChrome.vue`（两者硬编码认证域 API），自建精简阅读区块。
- **R26**：`describeShare()` 的字段是 `requiresPassword`，不是计划文本示例代码里的 `passwordProtected`。
- **R27**：`X-Share-Ticket` 头只在"带密码"分支需要。
- **R28**：视频分享的文件列表按 `files` 平铺渲染，`groups` 恒为空数组。
- **R29**：媒体库管理表格不显示"条目数"列（`LibraryDto.Response` 没有这个字段也没有对应端点）。
- **R30**：分片大小必须读服务端 `POST /api/upload/sessions` 响应里的 `chunkSize` 字段，不能客户端自定义。
- **R31**：新增后端只读端点 `GET /api/scrape/queue`（`ScrapeCandidateStore.distinctTargets`/`ScrapeCandidateService.pendingTargets`/`MetadataDto.QueueEntry`/`ScrapeCandidateController.queue()`），既有 `GET /api/scrape/candidates`/`confirm`/`ignore` 三个端点不变，纯加法、无迁移、无新增跨模块依赖边。
- **R32**：`api/admin.ts` 不重复定义 `listShares`/`revokeShare`（Task 9 的 `api/shares.ts` 已有）。
- **R33**：媒体库管理页的域徽标颜色写死字面量（`#ffb020`/`#c0483a`），不能用 `var(--accent)`——该页是中性壳，`<body>` 不带 `data-domain`。
- **R34**：`client.ts` 新增 `putBinary()` 发二进制分片，不能复用会 `JSON.stringify` body 的 `request()`。
- **R35**：`fieldSources`/`scrapeSource` 的真实取值是 `LocalNfo`/`Bangumi`/`TMDB`/`Filename`/`USER`（精确大小写），不是计划文本里的 `NFO`/`FILENAME`。
- **R36**：`chunker.test.ts` 用例数订正为 11 条（不是计划文本里的 14），累计 55 这个数没错。

### 三次 review 各自抓到的真实问题（均已修复并经 scoped re-review 确认）

1. **Task 16**：无密码路径下 `init()` 把"令牌无效"与"内容加载失败"混为一谈，且本次主动新增的图片容器节点父子导航（`enterChild`/`goUpNode`）完全没有错误处理，会产生未捕获 rejection。修法：拆分状态、统一走一个可重试的 `runLoad`/`lastLoad` 机制。
2. **Task 17**：两步建库流程（`createLibrary` 成功、`setMetadataProviders` 失败）共用一个 try/catch，会误导用户重复提交、建出无法删除的重复库（全仓库没有 `DELETE /api/libraries/{id}` 端点）。修法：拆成两个独立 try/catch，第二步失败时给出区分性提示、不清空表单、给刮削器单元格一个显式的"设置失败"状态。
3. **Task 18**：分片上传的合并阶段轮询（`pollUntilAssembled`）完全没有错误处理，续传路径尤其严重——刷新页面后会因为状态判断只认 `RECEIVING` 而清空本地续传记录、彻底失去对仍在合并的会话的可见性。修法：轮询自带有界重试与自归因错误，续传路径补齐 try/catch，`onMounted` 识别 `ASSEMBLING` 状态。

### 遗留 Minor（不阻塞，留给 Task 19 之后的 whole-branch final review 判断）

见本文件 §5 新增的 G25–G29（带密码分享不支持播放、图片分享的可读可浏览二选一简化、
两处 N+1 查询、刮削候选无封面、上传续传记录单一 key），以及 Task 16/17/18 各自
review 报告里记录的样式重复/键盘可达性/无障碍等更细碎的 Minor（明细见 SDD ledger，
不在此重复列出）。

### 下一步（本节写下时的状态，已被 §19 取代）

见 §19——Task 19 自己的 review 也经过一轮 fix（`2679b37`），随后 whole-branch final
review 已经做完并且其发现已修复。

## 19. 计划 07 全分支 final review 与收尾修复（2026-08-28）

- **工作区与状态**：`D:\MyMedia-7`，分支 `plan07-2026-08-17-07-frontend` 不变。**计划 07
  的全部 19 个任务、加 Task 19 自身的一轮 fix round、加一次全分支 final review 及其
  修复，到本节写下时全部完成**。HEAD `b65b98f`。仍**尚未合入 `main`**。
- **Task 19 自身的 fix round**：review 发现 §18 那次记录里留了一个没替换的占位符
  （`HEAD <Task 19 自己的提交，见下>`，且引用了一个不存在的"本节自身的提交"小节），
  修复提交 `2679b37`——上面 §18 表格里 Task 19 那一行的 `8deb25f` 因此不是这条任务链的
  最终提交，真正的最终提交是 `2679b37`（docs-only，两处占位符/措辞修正）。

### 全分支 whole-branch final review（MERGE_BASE `858da38` → `2679b37`，39 commits，model opus）

结论：**Ready to merge: With fixes**。审查确认了 `SecurityConfig` 三层改动叠加后
的最终态无误（`/actuator/health`→`/api/share/**`→静态资源白名单→`anyRequest().authenticated()`
互不遮蔽）、模块边界累计后确实没有新增依赖边（全程零 `package-info.java` 改动）、
两套视觉语言在中性页面上（除已知的两处遗漏，见下）都正确落地、媒体票据与分享票据
两套机制互不干扰、`srcFor` prop 最终收敛为 `PageView.vue` 的 `srcOverride` 是干净的
纯加法方案、零 Flyway 迁移。但抓到 2 条 Critical + 1 条 Important（其中 Important
审查员的原始表述有误，控制器已用 grep 亲自核实纠正范围）+ 6 条 Minor：

- **Critical 1**：`frontend/src/api/media.ts` 的 `assetUrl()` 依赖的模块级变量
  `latest` 只在应用启动那一刻、且仅当当时已有登录凭证时才预取一次，此后永不更新——
  正常登录流程（登录前没有凭证）下这个预取永远不会触发，**净效果是登录后封面几乎
  永远显示占位图**；即使某次启动时预取了，15 分钟后 `TicketCache` 内部续期对这个
  死快照也不可见，新渲染的封面会带着过期票据打 401。`Cover.vue` 的 `computed` 也
  不依赖任何响应式状态，即便后来更新 `latest` 也不会重渲染。
- **Critical 2**：`VideoPlayer.vue` 无条件每 5 秒上报一次播放进度（认证域端点
  `PUT /api/video/progress/{fileId}`），而 `ShareView.vue` 把它原样渲染给**无密码
  视频分享的匿名访客**——播放几秒后第一次上报就会 401，触发全局
  `unauthorizedHandler` 把访客登出并跳转 `/login`，打断整个分享体验。这是 Task 16
  preflight Ruling R25 的一处遗漏：R25 只检查了"地址解析要不要认证"，没检查组件
  内部还有一条无条件的认证域写路径（图片侧当时反而想到了同一类风险，才没有直接
  复用 `ReaderView`/`ReaderChrome`，视频侧的对称检查漏掉了）。
- **Important 3（审查员原始表述有误，已用 grep 核实纠正）**：审查员声称"分享创建
  端点在整个前端里完全没被调用、`/s/:token` 从应用内部不可达"——**核实为不准确**：
  `ItemDetailView.vue`（Task 9 产出）确实调用了 `createShare('video', ...)` 并展示
  完整链接 + 复制按钮，视频域的分享创建与访问都是可达的。真正成立的是两个更窄的点：
  (a) 图片域没有对称的分享创建入口（`NodeBrowseView.vue` 没有类似的分享面板，虽然
  后端 `POST /api/image/nodes/{id}/share` 端点存在）；(b) `LibraryAdminView.vue`
  的分享管理表格只显示令牌前 8 位，没有复制完整链接的入口。
- 6 条 Minor：`SecurityConfig.java` 的静态白名单写的是不存在的 `/favicon.ico` 而不是
  真正用到的 `/favicon.svg`（每个页面的图标请求都因此 401）；`SearchBox.vue`/
  `Cover.vue` 在中性页面上 `var(--elevation)` 没有兜底值（阴影静默消失）；
  `ShareView.vue` 的 `onBeforeUnmount` 无条件删除 `document.body.dataset.domain`，
  可能抹掉目标页面刚设好的视觉语言（因 Important 3 的纠正，这条从"潜伏"变成"当前
  就可复现"）；SPA 通配路由与安全白名单不对称（拼错的深链接会拿到裸 401）；
  `/api/scrape/queue` 对每个目标做了两次重复查询；一处注释与 `client.ts` 现状不符。

### 修复（唯一一轮——SDD 流程规定 final review 之后只有一次修复机会）

4 个提交，均已跑通前后端全量验证：

| 提交 | 内容 |
|---|---|
| `a002a99` | Critical 1：媒体票据状态改成响应式，`issue()` 收成唯一入口使 `TicketCache` 自动续期也能反映到 `assetUrl()`，登录成功后主动预热，新增 4 条回归测试（覆盖"无票据→有票据→失效→再次为空"全生命周期，含一条专门钉死"续期不可见"这个次生问题的用例）。捎带修了 `Cover.vue` 的 `--elevation` 兜底（Minor）。 |
| `d914acd` | Critical 2：`VideoPlayer.vue` 新增 `reportProgress?: boolean`（默认 `true`），四处上报调用点（`onTimeUpdate`/`onPause`/`onVisibilityChange`/`onBeforeUnmount`）全部受它门控；`ShareView.vue` 传 `false`；`PlayerView.vue`（登录用户正常播放）不受影响。捎带做了纵深防御——`main.ts` 注入的 401 回调现在对 `meta.anonymous` 路由不触发登出跳转；捎带修了 `ShareView.vue` 卸载时误删别的路由 `data-domain` 的问题（Minor）。 |
| `40f93ff` | Important 3(b)：`LibraryAdminView.vue` 的分享表格补了复制完整链接的入口。Important 3(a)：**不新建图片域分享创建 UI**（成本不小、最后一轮修复没有独立 review 兜底，风险偏高），只在 §5 新增 **G30** 记账这处不对称。捎带修了 `SearchBox.vue` 的 `--elevation` 兜底（Minor）。 |
| `b65b98f` | Minor：`SecurityConfig.java` 放行 `/favicon.svg`，`SpaRoutingTest` 补一条覆盖用例。 |

**明确跳过未修**（成本/风险不划算，已记录留给后续）：SPA 通配路由与安全白名单的
不对称；`/api/scrape/queue` 的二次查询（本分支已多次接受同类 N+1 权衡）。

**真实验证数字（修复后）**：前端 `npx vitest run` **8 files / 68 tests**（对比 Task 19
完成时的 64，净增 4——媒体票据生命周期那组回归测试）全通过，`vue-tsc -b --force`/
`vite build` 均 exit 0；后端 `mvn -B -ntp verify` **BUILD SUCCESS，Tests run: 683,
Failures: 0, Errors: 0, Skipped: 2**（对比 682，净增 1——新的 `favicon_不需要登录()`
用例）。

修复的 scoped re-review 已完成——**7 条（2 Critical + 2 Important + 3 Minor）全部
ADDRESSED，未发现本轮修复引入的新 Critical/Important**。审查员逐条独立核实（不只是
信报告）：`media.ts` 的票据状态确实是响应式 `ref` 且 `TicketCache` 的自动续期确实
可见、`Cover.vue` 的 computed 确实建立了真实的响应式依赖；`reportProgress` 四处调用
点全部受门控、`PlayerView.vue` 确认不受影响；G30 与既有 G1–G29 无编号冲突；分享
表格的复制入口真实可用；favicon 白名单测试设计合理。新增 1 条 Minor（不阻塞、当前
不可达）：`ShareView.vue` 卸载时"比对后再删"的 domain 属性防护在理论上还有一个
残余场景（SPA 内部导航到同一个 domain 字符串），但该组件目前没有任何指向自身的
`RouterLink`/`router.push`、也没有任何页面链接到 `/s/:token`，只能整页加载进入，
这个场景当前不可达。

**至此，全分支 whole-branch final review 及其唯一一轮修复全部收敛，无未决的
Critical/Important。**

### 下一步

1. 这个分支**从未合入 `main`**，`main` 目前仍停在计划 06 合并后的状态。是否合并、
   如何合并（fast-forward / squash / 保留分支 / 是否 push）需要用 `superpowers:
   finishing-a-development-branch` 走一遍，并且属于需要向用户请示的动作——控制器
   到本节写下时**尚未**执行这一步。
2. 新窗口若要继续，先读本文件 §2/§3/§5/§13/§14/§15/§16/§17/§18/§19，再读
   `.superpowers/sdd/2026-08-17-07-frontend/progress.md` 恢复 ledger 状态
   （ledger 里 Task 16 那段执行记录此前一度被错误地挪到了文件末尾、乱序，
   已经修正回正确的时间顺序，内容没有丢失）。
